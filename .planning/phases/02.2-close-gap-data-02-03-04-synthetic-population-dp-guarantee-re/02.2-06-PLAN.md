---
phase: 02.2
plan: 06
type: tdd
wave: 4
depends_on: [04]
files_modified:
  - packages/data-pipeline/src/synthetic_pop/dp_inject.py
  - packages/data-pipeline/src/synthetic_pop/export.py
  - packages/data-pipeline/src/synthetic_pop/__init__.py
  - packages/data-pipeline/tests/test_dp_inject.py
autonomous: true
requirements: [DATA-03, DATA-02]

must_haves:
  truths:
    - "DP proof execution produces a formal ε ≤ 1.0 guarantee via OpenDP .map(d_in=1)"
    - "DP noise is applied to population-level aggregates only — individual profiles remain unmodified"
    - "build_privacy_statement() generates a CNIL-compliant French-language privacy attestation"
    - "The complete pipeline produces population-v2025.1.json with SHA-256 hash and .meta.json sidecar containing dp_epsilon, dp_data_source, dp_proof_timestamp"
  artifacts:
    - path: "packages/data-pipeline/tests/test_dp_inject.py"
      provides: "8 TDD tests replacing Wave 0 stub — validates DP proof, budget, privacy statement"
    - path: "packages/data-pipeline/src/synthetic_pop/__init__.py"
      provides: "Updated with export_with_dp() pipeline entry point"
    - path: "packages/data-pipeline/dist/population-v2025.1.json"
      provides: "50K synthetic profiles as JSON records with orient=records"
    - path: "packages/data-pipeline/dist/population-v2025.1.meta.json"
      provides: "Sidecar with version, sha256, dp_epsilon, dp_proof_timestamp, dp_data_source"
  key_links:
    - from: "dp_inject.py inject_dp_privacy()"
      to: "export.py export_synthetic_population()"
      via: "dp_report is passed as parameter to export_synthetic_population"
      pattern: "export_synthetic_population.*dp_report"
    - from: "dp_inject.py prove_dp_guarantee()"
      to: "OpenDP dp.m.make_laplace().map(d_in=1)"
      via: "Formal ε proof via .map(d_in=1) — the auditable guarantee"
      pattern: "composed\\.map\\(d_in=1\\)"
    - from: "export.py .meta.json"
      to: "D-11 extended sidecar fields"
      via: "dp_data_source and dp_proof_timestamp added to sidecar"
      pattern: "dp_proof_timestamp"
    - from: "export_with_dp()"
      to: "Plan 04 generate_from_insee()"
      via: "Takes synthetic_df from Plan 04 output, runs DP inject + export"
      pattern: "generate_from_insee.*synthetic_df"
---

<objective>
Execute the differential privacy proof pipeline on the synthetic population produced by Plan 04: run prove_dp_guarantee() to formally prove ε ≤ 1.0 via OpenDP's .map(d_in=1), inject Laplace noise into population-level aggregates (decile boundaries, Gini coefficient) using inject_dp_privacy(), generate the CNIL-compliant French-language privacy statement via build_privacy_statement(), and export the complete population-v2025.1.json with SHA-256 hash and .meta.json sidecar containing dp_epsilon, dp_data_source, and dp_proof_timestamp per D-11.

Purpose: Close DATA-03 by producing the formal DP guarantee that proves the synthetic population meets CNIL requirements for differential privacy. This is the final step in the synthetic population pipeline — once executed, the 50K profiles are ready for consumption by the TypeScript engines.

Output: Working test_dp_inject.py with 8 TDD tests, updated __init__.py with export_with_dp() entry point, exported population-v2025.1.json + .meta.json sidecar.
</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-CONTEXT.md (D-08 through D-11)
@.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-RESEARCH.md (DP Proof Execution section, lines 500-523; Pitfall 2: DP Budget Exhaustion, lines 434-442; Don't Hand-Roll section, lines 398-408; Anti-Pattern: "Hand-rolling Laplace noise in NumPy")
@packages/data-pipeline/src/synthetic_pop/dp_inject.py (prove_dp_guarantee, inject_dp_privacy, build_privacy_statement — existing code, verified correct)
@packages/data-pipeline/src/synthetic_pop/export.py (export_synthetic_population, compute_sha256 — existing code, takes dp_report as parameter)
@packages/data-pipeline/src/synthetic_pop/__init__.py (extend with export_with_dp)
@packages/data-pipeline/tests/test_dp_inject.py (Wave 0 stub — replace with real tests)

<interfaces>
From dp_inject.py (existing, verified correct):
```python
def prove_dp_guarantee(data: list[float], epsilon_target: float = 1.0) -> dict
# Returns: {actual_epsilon, within_budget, noisy_sum, mechanism}

def inject_dp_privacy(synthetic_df: pd.DataFrame, epsilon_budget: float = 1.0) -> tuple
# Returns: (synthetic_df_unmodified, dp_report_dict)
# dp_report keys: epsilon_budget, mechanism, composition, aggregates, total_epsilon_consumed, within_budget

def build_privacy_statement(dp_report: dict, reference_year: int = 2025) -> str
# Returns: French-language CNIL-compliant privacy statement string
```

From export.py (existing, verified correct):
```python
def export_synthetic_population(synthetic_df: pd.DataFrame, dp_report: dict, output_path: str, reference_year: int = 2025) -> str
def compute_sha256(filepath: str) -> str
```

D-08: Execute dp_inject.py as a pipeline property — proof is valid regardless of data provenance.
D-09: Preserve ε budget allocation: ε/5 per revenu decile, ε/3 per patrimoine age bracket, ε/2 for Gini coefficient.
D-10: DP noise applied to aggregates only. Individual profiles remain unmodified.
D-11: Extend .meta.json with dp_data_source and dp_proof_timestamp. Export build_privacy_statement() as machine-readable JSON block.
</interfaces>
</context>

<feature>
  <name>DP Proof Execution + Population Export</name>
  <files>
    packages/data-pipeline/tests/test_dp_inject.py,
    packages/data-pipeline/src/synthetic_pop/__init__.py,
    packages/data-pipeline/src/synthetic_pop/dp_inject.py,
    packages/data-pipeline/src/synthetic_pop/export.py
  </files>
  <behavior>
    The dp_inject.py code is already complete and follows RESEARCH.md patterns correctly. This plan executes it against real synthetic data, validates the DP guarantees, and extends the export pipeline to include D-11 sidecar fields (dp_data_source, dp_proof_timestamp, privacy_statement).

    Test 1: prove_dp_guarantee() returns within_budget=True for a list of 1000 float values with epsilon_target=1.0
    Test 2: prove_dp_guarantee() returns within_budget=False when given 1,000,000 values (large n reduces sensitivity, but actual_epsilon still within 1.0 — test verifies the function computes epsilon correctly, not that it always passes)
    Test 3: inject_dp_privacy() returns synthetic_df UNMODIFIED (same DataFrame object, same values — D-10 contract)
    Test 4: inject_dp_privacy() dp_report has required top-level keys: epsilon_budget, mechanism, composition, aggregates, total_epsilon_consumed, within_budget
    Test 5: inject_dp_privacy() total_epsilon_consumed <= epsilon_budget (default budget=1.0) — verifies budget not exceeded
    Test 6: build_privacy_statement() returns a non-empty string containing "confidentialité différentielle" (CNIL terminology)
    Test 7: build_privacy_statement() string includes the ε value and mechanism name
    Test 8: export_synthetic_population() writes a .meta.json with dp_epsilon, dp_epsilon_budget, dp_within_budget, sha256 — and after D-11 extension: dp_data_source and dp_proof_timestamp fields present
  </behavior>
  <implementation>
    The implementation has four parts: (A) extend export.py to include D-11 sidecar fields, (B) add export_with_dp() to __init__.py, (C) replace Wave 0 test stubs with real tests, (D) validate dp_inject.py is correct (no changes needed).

    **Part A — Extend export.py .meta.json sidecar per D-11:**

    In `export_synthetic_population()`, after the existing meta dict (around line 95), add three fields:
    - `"dp_data_source"`: string — "OpenDP 0.14.2 Laplace mechanism with basic sequential composition. ε budget: 1.0 (ε/5 per revenu decile, ε/3 per patrimoine age bracket, ε/2 for Gini coefficient)."
    - `"dp_proof_timestamp"`: Use the same `datetime.now(timezone.utc).isoformat()` value as `generation_timestamp` (or a separate call — both use the same timestamp since they run in the same pipeline invocation).
    - `"privacy_statement"`: Call `build_privacy_statement(dp_report, reference_year)` and store the returned French-language string — this is the machine-readable CNIL attestation per D-11.

    **Part B — Add export_with_dp() to __init__.py:**

    Add a new public function that takes the output of `generate_from_insee()` (from Plan 04) and runs DP + export:

    ```python
    def export_with_dp(pipeline_result: dict, output_dir: Optional[str] = None) -> str:
        """
        Run DP injection on the synthetic population and export to JSON.
        
        Args:
            pipeline_result: dict from generate_from_insee() with keys:
                synthetic_df, quality_report, dp_report (optional)
            output_dir: Output directory for population JSON.
        
        Returns:
            Path to the exported population JSON file.
        """
    ```

    Implementation:
    1. Extract synthetic_df from pipeline_result
    2. Call inject_dp_privacy(synthetic_df, epsilon_budget=1.0) → (synthetic_df, dp_report)
    3. Log the dp_report summary: total_epsilon_consumed, within_budget
    4. Determine output_path: {output_dir}/population-v2025.1.json (default to packages/data-pipeline/dist/)
    5. Call export_synthetic_population(synthetic_df, dp_report, output_path, reference_year=2025)
    6. Build the complete privacy statement and log it
    7. Return the output path

    Add `"export_with_dp"` to `__all__`.

    **Part C — Replace test_dp_inject.py Wave 0 stub with real tests:**

    Replace the placeholder test with 8 real test functions:

    - `test_prove_dp_guarantee_within_budget()` — uses np.random.default_rng(42).normal(loc=30000, scale=15000, size=1000) as test data. Asserts result["within_budget"] is True.
    - `test_prove_dp_guarantee_empty_data()` — empty list returns within_budget=True, actual_epsilon=0.0, noisy_sum=0.0
    - `test_inject_dp_privacy_preserves_dataframe()` — creates a small test DataFrame (100 rows × 8 COLUMNS), calls inject_dp_privacy, asserts the returned DataFrame has same values as input (D-10: profiles unmodified)
    - `test_inject_dp_privacy_report_keys()` — verifies dp_report dict has all required keys
    - `test_inject_dp_privacy_budget_not_exceeded()` — asserts dp_report["total_epsilon_consumed"] <= dp_report["epsilon_budget"] (within numeric tolerance 1e-6)
    - `test_build_privacy_statement_content()` — creates a minimal dp_report dict, calls build_privacy_statement, asserts "confidentialité différentielle" is in the returned string, and the ε value appears
    - `test_build_privacy_statement_includes_mechanism()` — asserts "laplace" (case-insensitive) or "OpenDP" appears in the statement
    - `test_export_sidecar_dp_fields()` — creates test DataFrame + dp_report, calls export_synthetic_population to a tmp_path, reads the .meta.json, asserts dp_epsilon, dp_epsilon_budget, dp_within_budget, dp_data_source, dp_proof_timestamp, and privacy_statement fields are all present

    All tests use small in-memory DataFrames (50-500 rows) — no 50K data required for unit tests.

    **Part D — dp_inject.py validation (no changes needed):**

    The existing dp_inject.py code is correct per RESEARCH.md verification. Verify:
    - `prove_dp_guarantee()` uses `composed.map(d_in=1)` for formal ε proof (line 133) — NEVER hand-rolls NumPy Laplace
    - `inject_dp_privacy()` allocates budget as ε/5 per decile, ε/3 per age bracket, ε/2 for Gini (lines 184, 204, 226)
    - `build_privacy_statement()` references CNIL délibération n° 2021-122 (line 287)
    
    No code modifications needed in dp_inject.py. The module rule against hand-rolling Laplace noise (line 7-8) is correctly followed.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic_df → inject_dp_privacy() | Unvalidated DataFrame crosses into DP injection — must not contain PII from real individuals (it doesn't — synthetic data) |
| dp_report → .meta.json | DP proof metadata written to disk — must be tamper-evident via SHA-256 |
| .meta.json → methodology page (Phase 3) | DP attestation displayed to users — must be machine-verifiable |
| dp_inject.py OpenDP calls | Formal ε proof relies on OpenDP library integrity — pinned at 0.14.2 in pyproject.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-19 | Tampering | DP proof falsification (manual epsilon value written to .meta.json) | mitigate | .meta.json dp_epsilon is programmatically derived from OpenDP composed.map(d_in=1) — cannot be manually overridden without code change. Test 1 validates within_budget from actual OpenDP computation. SHA-256 hash in sidecar covers the JSON — any post-hoc .meta.json modification is detectable. |
| T-02.2-20 | Information Disclosure | DP noise insufficient to protect aggregates | mitigate | ε budget is pre-allocated at ε ≤ 1.0 with formal proof. D-10 ensures noise is on aggregates only. Test 5 validates total_epsilon_consumed ≤ epsilon_budget. Sequential composition via OpenDP make_basic_composition prevents budget double-spending. |
| T-02.2-21 | Tampering | population-v2025.1.json substitution | mitigate | SHA-256 hash in .meta.json computed over the JSON file content. Downstream PopulationCache.loadFromJSON() should verify hash before constructing cache per D-14. Phase 3 Service Worker with subresource integrity adds defense-in-depth. |
| T-02.2-22 | Repudiation | DP proof timestamp manipulation | mitigate | dp_proof_timestamp is set to UTC ISO 8601 at generation time via datetime.now(timezone.utc). The timestamp is recorded in the machine-readable .meta.json sidecar. Git commit timestamp on the artifact provides an independent verification source. |
</threat_model>

<verification>
TDD RED-GREEN-REFACTOR cycle:
1. RED: `python3.11 -m pytest packages/data-pipeline/tests/test_dp_inject.py -v` — tests FAIL (stub only)
2. GREEN: Implement export.py D-11 extensions + export_with_dp() in __init__.py, all 8 tests PASS
3. REFACTOR: Add type hints, docstrings. Tests must stay green.

Post-GREEN manual verification:
- Run full pipeline: `python3.11 -c "from synthetic_pop import generate_from_insee, export_with_dp; result = generate_from_insee(epochs=500); path = export_with_dp(result); print('Exported:', path)"` 
- Verify `ls -la packages/data-pipeline/dist/population-v2025.1.json` exists with ~50K records
- Verify `python3.11 -c "import json; m = json.load(open('packages/data-pipeline/dist/population-v2025.1.meta.json')); print('dp_epsilon:', m['dp_epsilon'], 'within_budget:', m['dp_within_budget'])"` — dp_epsilon ≤ 1.0, within_budget is True
- Verify `python3.11 -c "from synthetic_pop.dp_inject import prove_dp_guarantee; import numpy as np; rng = np.random.default_rng(42); data = list(rng.normal(30000, 15000, 1000)); proof = prove_dp_guarantee(data); print('actual_epsilon:', proof['actual_epsilon'], 'within_budget:', proof['within_budget'])"` — within_budget is True
</verification>

<success_criteria>
- [ ] 8 TDD tests pass (`pytest packages/data-pipeline/tests/test_dp_inject.py -v` exits 0)
- [ ] `prove_dp_guarantee()` returns `within_budget=True` for realistic test data (Test 1)
- [ ] `inject_dp_privacy()` does NOT modify individual profiles (Test 3 — D-10 contract)
- [ ] `inject_dp_privacy()` total_epsilon_consumed ≤ epsilon_budget (Test 5)
- [ ] `build_privacy_statement()` generates French-language CNIL-compliant statement (Tests 6, 7)
- [ ] `export_synthetic_population()` writes .meta.json with dp_data_source, dp_proof_timestamp, and privacy_statement fields (Test 8 + D-11)
- [ ] `export_with_dp()` function exists in `__init__.py` and is exported in `__all__`
- [ ] Full pipeline execution produces `population-v2025.1.json` and `.meta.json` with valid DP proof
- [ ] dp_inject.py code is NOT modified (already correct per RESEARCH.md verification)
- [ ] Existing export.py functions (export_synthetic_population, compute_sha256) remain backward-compatible
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-06-SUMMARY.md`
</output>
