---
phase: 02.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/engine/types.ts
  - webapp/src/engine/__tests__/population-cache.test.ts
  - webapp/src/engine/__tests__/shock-matrix-integration.test.ts
  - packages/data-pipeline/tests/test_dp_inject.py
  - packages/data-pipeline/tests/test_insee_loader.py
  - packages/data-pipeline/tests/test_calibrate.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "Python 3.11 venv exists with SDV 1.36.1, OpenDP 0.14.2, SDMetrics installed and importable"
    - "TypeScript types.ts exports Profile and PopulationDoc interfaces matching the JSON schema"
    - "All Wave 0 test stubs exist with valid file structure and a single passing placeholder test each"
  artifacts:
    - path: "webapp/src/engine/types.ts"
      provides: "Profile and PopulationDoc interfaces"
    - path: "webapp/src/engine/__tests__/population-cache.test.ts"
      provides: "Wave 0 stub with describe/it/expect imports"
    - path: "webapp/src/engine/__tests__/shock-matrix-integration.test.ts"
      provides: "Wave 0 stub with describe/it/expect imports"
    - path: "packages/data-pipeline/tests/test_dp_inject.py"
      provides: "Wave 0 stub with pytest function"
    - path: "packages/data-pipeline/tests/test_insee_loader.py"
      provides: "Wave 0 stub with pytest function"
    - path: "packages/data-pipeline/tests/test_calibrate.py"
      provides: "Wave 0 stub with pytest function"
  key_links:
    - from: "webapp/src/engine/types.ts"
      to: "packages/data-pipeline/src/schemas/synthetic_profile.schema.json"
      via: "Profile interface must match the 10 JSON Schema fields"
      pattern: "Profile references schema field names"
    - from: "PopulationDoc (in types.ts)"
      to: "webapp/src/engine/population-cache.ts (future)"
      via: "PopulationCache.fromDoc() consumes PopulationDoc"
      pattern: "interface PopulationDoc"
---

<objective>
Set up the Python environment for the offline data pipeline (resolve SDV/OpenDP Python version blocking issue), define the TypeScript type contracts (Profile, PopulationDoc) that all downstream TypeScript plans consume, and create Wave 0 test stubs per VALIDATION.md so that TDD plans in Wave 2 can replace stubs with real tests.

Purpose: Unblock all Python pipeline work (SDV 1.36.1 fails on Python 3.13) and establish the interface contracts that Plans 02.2-04 (PopulationCache), 02.2-05 (pipeline execution), and 02.2-06 (worker integration) build against.
Output: Working Python 3.11 venv with all dependencies installed, extended types.ts with Profile/PopulationDoc interfaces, 5 Wave 0 test stub files.
</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-CONTEXT.md
@.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-RESEARCH.md
@.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-VALIDATION.md

<interfaces>
Key types the executor needs — extracted from the codebase to avoid scavenger hunting:

From webapp/src/engine/types.ts (current state):
```typescript
export interface ScenarioResult { ir, is, tva, cotisations, aides, revenuDisponible }
export interface ScenarioDefinition { id, name, description, parameterOverrides }
export interface ShockMatrixData { grid: Float64Array, taxBp: Float64Array, spendBp: Float64Array, horizonBp: Float64Array, hullEquations: Float64Array }
export interface WorkerRequest { id, type, payload }
export interface WorkerResponse<T> { id, type, payload }
export interface CitizenInitPayload { scenariosJson: string }
export type WorkerMessageType = 'INIT' | 'SIMULATE' | 'INTERPOLATE' | 'PROJECT' | 'READY' | 'CITIZEN_RESULT' | 'MACRO_RESULT' | 'ERROR'
```

From packages/data-pipeline/src/schemas/synthetic_profile.schema.json (JSON Schema Draft 2020-12):
Required fields: profile_id (string), age (integer 0-120), patrimoine (number >=0), revenu_fiscal (number), situation_familiale (enum: celibataire/marie/pacse/veuf/divorce/separe), nombre_parts (number 1-20)
Optional fields: type_activite (enum), zone_residence (enum: zone1/zone2/zone3), enfants_a_charge (integer 0-20), loyer_mensuel (number >=0), statut_occupation (enum: proprietaire/locataire/heberge_gratuitement)

From webapp/src/engine/scenario-cache.ts:
```typescript
export interface ScenarioDoc { definition: ScenarioDefinition; results: Record<string, ScenarioResult> }
export class ScenarioCache {
  lookup(scenarioId: string, profileIndex: number): ScenarioResult | undefined
  listScenarios(): ScenarioDefinition[]
  addScenario(doc: ScenarioDoc): void
  static fromDocs(docs: ScenarioDoc[]): ScenarioCache
  static async loadFromJSON(url: string): Promise<ScenarioCache>
}
```

From packages/data-pipeline/src/synthetic_pop/export.py:
- export_synthetic_population(synthetic_df, dp_report, output_path, reference_year=2025) → str
- compute_sha256(filepath) → str (64-char hex)
- .meta.json sidecar keys: version, reference_year, num_profiles, dp_epsilon, dp_epsilon_budget, dp_within_budget, sha256, generation_timestamp, data_source
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve SDV Python version — create Python 3.11 venv and install dependencies</name>
  <files>packages/data-pipeline/.venv/ (recreated), packages/data-pipeline/pyproject.toml</files>
  <read_first>
    - .planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-RESEARCH.md (lines 422-433 for Pitfall 1: SDV version conflict, lines 632-653 for Environment Availability)
    - packages/data-pipeline/pyproject.toml (for existing dependency pins)
  </read_first>
  <action>
    Per RESEARCH.md Environment Availability section and Pitfall 1: SDV 1.36.1 fails pip install on Python 3.13.13 with ResolutionImpossible. The fix is to create a new venv with Python 3.11 (the most likely compatible version for SDV 1.x).

    1. Check if Python 3.11 is available: `python3.11 --version` or `pyenv versions`. If not installed, install via `brew install python@3.11` or `pyenv install 3.11`.

    2. Remove the existing .venv: `rm -rf packages/data-pipeline/.venv`

    3. Create new venv with Python 3.11: `python3.11 -m venv packages/data-pipeline/.venv`

    4. Activate and install dependencies: `source packages/data-pipeline/.venv/bin/activate && pip install --upgrade pip setuptools wheel`

    5. Install the three blocked packages: `pip install sdv==1.36.1 opendp==0.14.2 "sdmetrics>=0.21,<2.0"`

    6. Verify imports work: `python -c "import sdv; import opendp; import sdmetrics; print('SDV', sdv.__version__); print('OpenDP', opendp.__version__)"`

    7. If SDV 1.36.1 also fails on Python 3.11, try Python 3.10. If all fail, evaluate SDV 2.x API compatibility with train.py (which uses CopulaGANSynthesizer, SingleTableMetadata from SDV 1.x API). If migration needed, document the required API changes but do NOT implement them — create a checkpoint.

    8. Verify the existing packages remain: `pip list | grep -E "numpy|scipy|pandas|pyarrow|statsmodels"` — these were already installed and should survive the venv recreation.
  </action>
  <acceptance_criteria>
    - `python3.11 -c "import sdv; print(sdv.__version__)"` outputs `1.36.1` (or compatible version)
    - `python3.11 -c "import opendp; print(opendp.__version__)"` outputs `0.14.2`
    - `python3.11 -c "import sdmetrics; print(sdmetrics.__version__)"` succeeds with >=0.21
    - `python3.11 -c "import numpy; import scipy; import pandas; import pyarrow; print('core OK')"` succeeds
    - `grep -c "sdv==1.36.1" packages/data-pipeline/pyproject.toml` returns 1 (or updated version if migration needed)
  </acceptance_criteria>
  <done>All three previously-blocked dependencies (SDV 1.36.1, OpenDP 0.14.2, SDMetrics 0.21+) are importable in the Python 3.11 venv.</done>
</task>

<task type="auto">
  <name>Task 2: Define Profile and PopulationDoc TypeScript interfaces</name>
  <files>webapp/src/engine/types.ts</files>
  <read_first>
    - webapp/src/engine/types.ts (current state — add interfaces at the end, do not modify existing types)
    - packages/data-pipeline/src/schemas/synthetic_profile.schema.json (canonical schema — Profile interface must match these 10 fields)
    - .planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-RESEARCH.md (lines 251-323 for PopulationDoc interface design from Pattern 1)
    - webapp/src/engine/scenario-cache.ts (for ScenarioDoc pattern that PopulationDoc mirrors — same version/reference_year/meta structure)
  </read_first>
  <action>
    Add two new interfaces to webapp/src/engine/types.ts, placed after the existing ScenarioResult interface (around line 59) and before ShockMatrixData. Follow the exact RESEARCH.md Pattern 1 design (lines 253-265).

    Interface 1 — Profile: mirrors the 10 fields from synthetic_profile.schema.json (Draft 2020-12). Required fields: profile_id (string), age (number), patrimoine (number), revenu_fiscal (number), situation_familiale (string union), nombre_parts (number). Optional fields: type_activite, zone_residence, enfants_a_charge, loyer_mensuel, statut_occupation. Use union types for enum fields.

    Interface 2 — PopulationDoc: wraps the JSON structure consumed by PopulationCache.fromDoc(). Fields:
    - version: string (e.g. "population-v2025.1")
    - reference_year: number
    - profiles: Profile[]
    - meta: object with dp_epsilon (number), dp_data_source (string), dp_proof_timestamp (string), sha256 (string)

    Add a new WorkerMessageType union member: 'INIT_POPULATION' to the existing WorkerMessageType type (around line 110).

    Add a new payload interface: PopulationInitPayload with populationJson: string.

    Export all new interfaces.
  </action>
  <acceptance_criteria>
    - `grep -c "export interface Profile" webapp/src/engine/types.ts` returns 1
    - `grep -c "export interface PopulationDoc" webapp/src/engine/types.ts` returns 1
    - `grep -c "export interface PopulationInitPayload" webapp/src/engine/types.ts` returns 1
    - `grep "profile_id" webapp/src/engine/types.ts` returns at least 1 (Profile interface includes profile_id field)
    - `grep "INIT_POPULATION" webapp/src/engine/types.ts` returns at least 1 (WorkerMessageType includes new variant)
    - `grep "situation_familiale" webapp/src/engine/types.ts` returns at least 1 (union type for family situation)
    - Existing exports (ScenarioResult, ScenarioDefinition, ShockMatrixData, MacroResult, WorkerRequest, etc.) are NOT modified — verify: `grep "export interface ScenarioResult" webapp/src/engine/types.ts` returns 1
  </acceptance_criteria>
  <done>TypeScript types.ts compiles without errors (check: `npx tsc --noEmit webapp/src/engine/types.ts`), exports Profile, PopulationDoc, PopulationInitPayload, and INIT_POPULATION variant.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test stubs per VALIDATION.md</name>
  <files>
    webapp/src/engine/__tests__/population-cache.test.ts,
    webapp/src/engine/__tests__/shock-matrix-integration.test.ts,
    packages/data-pipeline/tests/test_dp_inject.py,
    packages/data-pipeline/tests/test_insee_loader.py,
    packages/data-pipeline/tests/test_calibrate.py
  </files>
  <read_first>
    - .planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-VALIDATION.md (lines 52-58 for Wave 0 requirements listing exactly which files need stubs)
    - webapp/src/engine/__tests__/scenario-cache.test.ts (template for vitest test structure: describe/it/expect pattern, test fixture helpers)
    - webapp/src/engine/types.ts (for Profile/PopulationDoc imports in new test files)
    - packages/data-pipeline/tests/test_synthetic_pop.py (template for pytest test structure)
  </read_first>
  <action>
    Create 5 Wave 0 test stub files following VALIDATION.md Wave 0 requirements (lines 54-58). Each stub has: proper imports, one placeholder test that always passes (assert True in Python, expect(true).toBe(true) in TypeScript), and a TODO comment listing the real tests that will be added in later plans.

    File 1 — webapp/src/engine/__tests__/population-cache.test.ts:
    - Import: describe, it, expect from vitest; Profile, PopulationDoc from types; PopulationCache from population-cache (future import — use a commented-out import with TODO)
    - One test: `it('Wave 0 placeholder', () => { expect(true).toBe(true); })`
    - TODO comment listing tests to add: loads JSON via fromDoc(), getProfile returns correct profile, getByDecile returns profiles by decile, getByAgeGroup returns profiles by age group, getProfileCount returns 50000, getMeta returns metadata

    File 2 — webapp/src/engine/__tests__/shock-matrix-integration.test.ts:
    - Import: describe, it, expect from vitest; MacroResult from types; interpolateAtPoint, projectTrajectory from macro-interpolate
    - One placeholder test
    - TODO comment listing tests: grid dimensions match 12x12x12, grid-center interpolation returns non-null, out-of-bounds returns null, trajectory projection returns 5 years

    File 3 — packages/data-pipeline/tests/test_dp_inject.py:
    - Import: pytest; from synthetic_pop.dp_inject import prove_dp_guarantee, inject_dp_privacy, build_privacy_statement
    - One test: `def test_wave0_placeholder(): pass`
    - TODO listing tests: prove_dp_guarantee returns within_budget=True, inject_dp_privacy produces valid report, build_privacy_statement returns French-language CNIL statement, epsilon budgeting correct

    File 4 — packages/data-pipeline/tests/test_insee_loader.py:
    - Import: pytest; import pandas as pd; from synthetic_pop.insee_loader import InseeAggregateLoader (future import — use try/except ImportError)
    - One placeholder test
    - TODO listing tests: constructs DataFrame with canonical COLUMNS, produces 50000 rows, handles missing source gracefully, all required schema fields present

    File 5 — packages/data-pipeline/tests/test_calibrate.py:
    - Import: pytest; import numpy as np; from shock_matrix.calibrate import calibrate_shock_matrix (future import — use try/except)
    - One placeholder test
    - TODO listing tests: grid has expected shape (12x12x12x4), non-null values at grid points, breakpoint ranges match D-09 defaults, convex hull covers all grid points

    All .ts files: follow the exact describe/it/expect pattern from scenario-cache.test.ts (vitest, no beforeEach needed for stubs).
    All .py files: follow pytest conventions (functions starting with test_, no classes).
  </action>
  <acceptance_criteria>
    - `ls webapp/src/engine/__tests__/population-cache.test.ts` exists
    - `ls webapp/src/engine/__tests__/shock-matrix-integration.test.ts` exists
    - `ls packages/data-pipeline/tests/test_dp_inject.py` exists
    - `ls packages/data-pipeline/tests/test_insee_loader.py` exists
    - `ls packages/data-pipeline/tests/test_calibrate.py` exists
    - `npx vitest run --dir webapp/src/engine/__tests__/ population-cache.test.ts shock-matrix-integration.test.ts` exits 0 (placeholder tests pass)
    - `python3.11 -m pytest packages/data-pipeline/tests/test_dp_inject.py packages/data-pipeline/tests/test_insee_loader.py packages/data-pipeline/tests/test_calibrate.py -v` exits 0 (placeholder tests pass)
    - Each file contains a TODO comment listing real tests to add (verify: grep "TODO" on each file returns at least 1 match)
  </acceptance_criteria>
  <done>All 5 Wave 0 stubs exist, import correctly, and pass with placeholder assertions. Ready for TDD plans (02.2-02, 02.2-03, 02.2-04) to replace stubs with real tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python venv → pip registry | Package integrity during pip install — verify hashes where available |
| TypeScript types ↔ JSON data | Deserialization boundary — Profile interface must match JSON schema exactly to avoid runtime type confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-01 | Tampering | pip install sdv/opendp/sdmetrics | accept | pip registry integrity is outside our threat model for offline pipeline packages. SDV/OpenDP are pinned to exact versions. No runtime exposure — packages run in local venv only. |
| T-02.2-02 | Information Disclosure | types.ts Profile interface | mitigate | Profile interface includes revenu_fiscal, patrimoine fields. The interface is a contract only — no actual data flows through types.ts at definition time. Runtime PopulationCache never exposes individual profiles to network per D-12. |
| T-02.2-03 | Spoofing | Wave 0 test stubs | accept | Placeholder tests have no assertions — they are replaced by real tests in Wave 2 TDD plans. No runtime impact. |
</threat_model>

<verification>
- Run `python3.11 -c "import sdv; import opendp; import sdmetrics"` — all three import without error
- Run `npx tsc --noEmit webapp/src/engine/types.ts` — no type errors
- Run `npx vitest run --dir webapp/src/engine/__tests__/` — all test files found, Wave 0 stubs pass
- Run `python3.11 -m pytest packages/data-pipeline/tests/test_dp_inject.py packages/data-pipeline/tests/test_insee_loader.py packages/data-pipeline/tests/test_calibrate.py -v` — all stubs pass
</verification>

<success_criteria>
- [ ] Python 3.11 venv exists with `sdv==1.36.1`, `opendp==0.14.2`, `sdmetrics>=0.21` installed and importable
- [ ] `types.ts` exports `Profile`, `PopulationDoc`, `PopulationInitPayload` interfaces
- [ ] `WorkerMessageType` includes `'INIT_POPULATION'` variant
- [ ] 5 Wave 0 test stubs created with placeholder tests that pass
- [ ] No existing code or tests modified — only additions
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re/02.2-01-SUMMARY.md`
</output>
