---
phase: 02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re
plan: 05
subsystem: engine
tags: [typescript, vitest, hashmap, population-cache, synthetic-data]

# Dependency graph
requires:
  - phase: 02.2-01
    provides: Profile, PopulationDoc interfaces in types.ts
provides:
  - PopulationCache class with O(1) HashMap lookup
  - Pre-indexed decile (1-10) and age-group (jeune/actif/senior/retraite) buckets
  - Copy-on-read accessor safety (spread arrays)
  - loadFromJSON() async factory for main-thread use
  - 15 passing vitest tests including performance benchmark
affects: [02.2-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Map<string, Profile> for O(1) lookup (mirrors ScenarioCache)"
    - "Decile boundaries via revenu_fiscal percentile rank sort"
    - "Age groups: jeune<35, actif<50, senior<65, retraite>=65"

key-files:
  created:
    - webapp/src/engine/population-cache.ts
  modified:
    - webapp/src/engine/__tests__/population-cache.test.ts

key-decisions:
  - "Decile computation uses percentile rank sort (O(n log n) at construction) for simplicity"
  - "Age group labels in French (jeune, actif, senior, retraite) for consistency with the French UI target audience"
  - "Copy-on-read via spread ([...arr]) — same mutation safety pattern as ScenarioCache"

patterns-established:
  - "Pattern 1: Mirror ScenarioCache architecture — private constructor, static fromDoc/loadFromJSON, Map-based storage"
  - "Pattern 2: Pre-indexed accessor buckets for decile and age-group queries"

requirements-completed: [DATA-02]

# Metrics
duration: 4 min
completed: 2026-05-15
---

# Phase 02.2 Plan 05: PopulationCache Summary

**O(1) HashMap PopulationCache class with decile/age-group pre-indexing — 15 vitest tests pass, performance contract validated (<1ms lookup on 50K profiles)**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-15T19:35:00Z
- **Completed:** 2026-05-15T19:39:00Z
- **Tasks:** 1 feature (TDD RED-GREEN-REFACTOR)
- **Files modified:** 2

## Accomplishments

- PopulationCache class with O(1) Map-based profile lookup by profile_id
- Decile indexing: profiles sorted by revenu_fiscal, assigned to deciles 1-10 via percentile boundaries
- Age group indexing: profiles binned into jeune (<35), actif (35-49), senior (50-64), retraite (65+)
- Copy-on-read safety: all accessors return [...bucket] spread arrays (no mutation of internal state)
- loadFromJSON() async factory for main-thread orchestrated loading
- Performance contract met: profile lookup on 50K cache completes in <0.1ms

## TDD Cycle Commits

1. **RED** — `2710021` — `test(02.2-05): add failing tests for PopulationCache`
2. **GREEN+REFACTOR** — `8b3a4e1` — `feat(02.2-05): implement PopulationCache`

## Files Created/Modified

- `webapp/src/engine/population-cache.ts` — PopulationCache class with Map-based storage, decile/ageGroup pre-indexing, copy-on-read, loadFromJSON
- `webapp/src/engine/__tests__/population-cache.test.ts` — 15 vitest tests (lookups, deciles, age groups, metadata, empty cache, mutation safety, performance)

## Decisions Made

- Decile computation uses simple percentile rank sort (O(n log n) at construction) — sufficient for 50K profiles, no external libraries needed
- Age group labels use French terms for UI consistency with the French-speaking target audience
- Copy-on-read via spread operator — same mutation safety pattern as ScenarioCache.listScenarios()

## Deviations from Plan

None — plan executed exactly as written with TDD cycle.

## Issues Encountered

None.

## Next Phase Readiness

- Ready for Plan 02.2-07 (WorkerOrchestrator integration — wiring PopulationCache into the INIT protocol)
- Exposes the same fromDoc/loadFromJSON API pattern as ScenarioCache for seamless worker integration

---
*Phase: 02.2-close-gap-data-02-03-04-synthetic-population-dp-guarantee-re*
*Completed: 2026-05-15*
