---
phase: 03-interactive-simulation-shell-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/package.json
  - webapp/tsconfig.json
  - webapp/vite.config.ts
  - webapp/index.html
  - webapp/src/main.tsx
  - webapp/src/index.css
  - webapp/vitest.config.ts
  - webapp/playwright.config.ts
  - webapp/public/data/.gitkeep
autonomous: true
requirements: [UI-06, A11Y-06]
must_haves:
  truths:
    - "npm install completes without errors"
    - "npm run dev starts a Vite dev server on localhost"
    - "npm run build produces a production bundle"
    - "vitest.config.ts exists and npx vitest run exits 0 (no tests yet)"
    - "playwright.config.ts exists and references web server config"
  artifacts:
    - path: "webapp/package.json"
      provides: "All Phase 3 dependencies declared with pinned versions"
      contains: "react@^19.2"
    - path: "webapp/vite.config.ts"
      provides: "Vite 6+ config with React plugin + Tailwind plugin"
      contains: "@tailwindcss/vite"
    - path: "webapp/src/index.css"
      provides: "Tailwind 4.3 @theme with Budget Citoyen design tokens"
      contains: "--color-primary: #1E3A5F"
    - path: "webapp/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment"
      contains: "environment: 'jsdom'"
    - path: "webapp/playwright.config.ts"
      provides: "Playwright configuration with webServer and axe-core"
  key_links:
    - from: "webapp/src/main.tsx"
      to: "webapp/src/index.css"
      via: "import './index.css'"
      pattern: "import.*index\\.css"
    - from: "webapp/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "tailwindcss"
---

<objective>
Bootstrap the Budget Citoyen frontend project: a Vite 6 + React 19 + TypeScript 5.8 + Tailwind CSS 4.3 build system with Vitest 4 and Playwright 1.60 test infrastructure. Creates all configuration files, entry points, and design tokens needed by all subsequent Phase 3 plans.

Purpose: Every other plan depends on the build system existing — `npm install` must work, `npm run dev` must start, imports must resolve, and Tailwind utilities must be available throughout the component tree.
Output: Fully functional `webapp/` build system with dependency manifest, TypeScript config, Vite config, HTML entry, React mount, Tailwind theme, Vitest config, and Playwright config.
</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-CONTEXT.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-RESEARCH.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-UI-SPEC.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md

<interfaces>
<!-- Key conventions for executors — no codebase exploration needed. -->

From UI-SPEC.md (Design System):
```
Tool: none (no shadcn/Radix)
Component library: React Aria hooks (useSlider, useButton, useToggle, useAccordion)
Icon library: lucide-react
Font: System font stack (-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, ...)
No web font download.

Spacing scale: xs=4px, sm=8px, md=16px, lg=24px, xl=32px, 2xl=48px, 3xl=64px
Exceptions: touch-target=44px, panel-width=380px (Tailwind arbitrary values)
```

From UI-SPEC.md (Color Palette):
```
Dominant (60%): #F8FAFC (page background)
Secondary (30%): #F0F4F8 (cards, sidebar, nav)
Accent (10%): #1E3A5F (buttons, slider thumb, active states, focus rings)
Destructive: #DC2626 (reset hover, error icons/text)
Text primary: #0F172A
Text secondary: #475569
Text on accent: #FFFFFF
Text disabled: #94A3B8
Chart palette (deuteranopia-safe): #0072B2, #E69F00, #009E73, #CC79A7
```

From UI-SPEC.md (Typography):
```
Body: 16px/400/1.5 — text-base font-normal leading-relaxed
Label: 14px/400/1.4 — text-sm font-normal leading-snug
Heading: 22px/600/1.3 — text-xl font-semibold leading-tight
Display: 32px/600/1.2 — text-3xl font-semibold leading-none
```

From RESEARCH.md (Dependency Versions — all verified via npm view 2026-05-13):
```
dependencies:
  react@^19.2 react-dom@^19.2
  react-aria-components lucide-react react-router-dom@^7
  vega-lite@^6.4 vega-embed@^7
  workbox-precaching workbox-routing workbox-strategies

devDependencies:
  vite@^6 typescript@^5.8 @vitejs/plugin-react@^6
  tailwindcss@^4.3 @tailwindcss/vite@^4
  vitest@^4 @playwright/test@^1 jsdom@^26
  @axe-core/playwright@^4
  workbox-build
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webapp/package.json with all pinned dependencies</name>
  <files>webapp/package.json</files>
  <read_first>
webapp/package.json — will be created, verify directory exists first
@.planning/phases/03-interactive-simulation-shell-mvp/03-RESEARCH.md — lines 62-73 for exact dependency list and versions
@.planning/phases/03-interactive-simulation-shell-mvp/03-UI-SPEC.md — lines 16-26 (design system decisions, no-shadcn rationale)
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md — lines 10-11 (no existing webapp/package.json)
  </read_first>
  <acceptance_criteria>
    <behavior>npm install in webapp/ directory completes without errors and installs all packages from package.json</behavior>
    <source>package.json contains `"react": "^19.2"`</source>
    <source>package.json contains `"react-aria-components"` in dependencies</source>
    <source>package.json contains `"vega-lite": "^6.4"` in dependencies</source>
    <source>package.json contains `"tailwindcss": "^4.3"` in devDependencies</source>
    <source>package.json contains `"@playwright/test": "^1"` in devDependencies</source>
    <source>package.json contains `"@axe-core/playwright": "^4"` in devDependencies</source>
    <source>package.json `"scripts"` block includes: `"dev": "vite"`, `"build": "tsc && vite build"`, `"preview": "vite preview"`, `"test": "vitest run"`, `"test:e2e": "playwright test"`</source>
    <cli>npm install --dry-run exits 0 (parses dependency tree without errors)</cli>
  </acceptance_criteria>
  <action>
Create webapp/package.json as the npm dependency manifest for the Budget Citoyen frontend. All version ranges come from RESEARCH.md lines 62-73 (verified via npm view 2026-05-13).

Set `"name": "budget-citoyen"`, `"private": true`, `"type": "module"`.

Dependencies (exact ranges from RESEARCH.md):
- react ^19.2, react-dom ^19.2
- react-aria-components (latest compatible — npm will resolve to 1.17.0+)
- lucide-react
- react-router-dom ^7
- vega-lite ^6.4, vega-embed ^7
- workbox-precaching, workbox-routing, workbox-strategies (all Workbox 7.4.x)

DevDependencies (exact ranges from RESEARCH.md):
- vite ^6, typescript ^5.8, @vitejs/plugin-react ^6
- tailwindcss ^4.3, @tailwindcss/vite ^4
- vitest ^4, @playwright/test ^1, jsdom ^26
- @axe-core/playwright ^4
- workbox-build

Scripts block:
- "dev": "vite" (starts dev server)
- "build": "tsc -b && vite build" (type-check then build)
- "preview": "vite preview" (preview production build)
- "test": "vitest run" (unit tests)
- "test:watch": "vitest" (watch mode)
- "test:e2e": "playwright test" (E2E + a11y tests)
- "lint": "tsc --noEmit" (type-check only)

Do NOT include engines or os restrictions. Do NOT use a package-lock.json — let npm generate it.
  </action>
  <verify>
    <automated>cd webapp && npm install 2>&1 | tail -1</automated>
  </verify>
  <done>npm install completes; all dependencies from RESEARCH.md are present; scripts block contains dev/build/test/test:e2e</done>
</task>

<task type="auto">
  <name>Task 2: Create Vite + TypeScript + Tailwind config files and entry points</name>
  <files>webapp/tsconfig.json, webapp/vite.config.ts, webapp/index.html, webapp/src/main.tsx, webapp/src/index.css</files>
  <read_first>
webapp/tsconfig.json — new file
webapp/vite.config.ts — new file
webapp/index.html — new file
webapp/src/main.tsx — new file
webapp/src/index.css — new file
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md — lines 16-18 (tsconfig.json), 19 (vite.config.ts), 20 (index.html), 21 (main.tsx), 22 (index.css) for scaffolding notes; lines 413-437 (main.tsx analog), lines 1227-1238 (index.css @theme analog)
@.planning/phases/03-interactive-simulation-shell-mvp/03-UI-SPEC.md — lines 30-97 (spacing scale, typography, color palette, text colors, chart palette for @theme)
@.planning/phases/03-interactive-simulation-shell-mvp/03-RESEARCH.md — lines 30-41 (Standard Stack), lines 158-234 (Recommended Project Structure)
  </read_first>
  <acceptance_criteria>
    <behavior>npx vite (from webapp/) starts a dev server and serves index.html at localhost:5173</behavior>
    <source>tsconfig.json contains `"jsx": "react-jsx"` and `"moduleResolution": "bundler"` and `"target": "ES2022"`</source>
    <source>vite.config.ts imports `@vitejs/plugin-react` and `tailwindcss` from `@tailwindcss/vite` in its plugins array</source>
    <source>index.html contains `<div id="root"></div>` and `<script type="module" src="/src/main.tsx"></script>`</source>
    <source>src/main.tsx imports App from ./App and calls createRoot with StrictMode wrapping</source>
    <source>src/index.css has `@import "tailwindcss"` as its first line</source>
    <source>src/index.css has `@theme { }` block containing `--color-primary: #1E3A5F` and `--font-sans:` with system font stack</source>
    <source>src/index.css @theme contains all color tokens from UI-SPEC: --color-primary (#1E3A5F), --color-secondary (#F0F4F8), --color-accent (#0072B2), --color-destructive (#DC2626), --color-text-primary (#0F172A), --color-text-secondary (#475569), --color-text-disabled (#94A3B8), --color-chart-deficit (#0072B2), --color-chart-debt (#E69F00), --color-chart-gdp (#009E73), --color-chart-employment (#CC79A7)</source>
    <source>src/index.css @theme contains `--spacing-touch-target: 44px` and `--spacing-panel-width: 380px`</source>
    <cli>npx tsc --noEmit (from webapp/) exits 0 after creating a minimal placeholder App.tsx</cli>
  </acceptance_criteria>
  <action>
Create 5 files following the established Phase 2 file banner convention (3-line `// webapp/src/path/file.ext` comment with purpose and decision reference):

**tsconfig.json:** Standard Vite + React + TypeScript config. Key settings:
- compilerOptions: target ES2022, lib ["ES2022","DOM","DOM.Iterable"], module ESNext, moduleResolution bundler, jsx react-jsx, strict true, noEmit true, isolatedModules true, skipLibCheck true, allowImportingTsExtensions true
- include: ["src"], references: [] (no project references needed)

**vite.config.ts:** Import `tailwindcss` from `@tailwindcss/vite` and `react` from `@vitejs/plugin-react`. Export default defineConfig with:
- plugins: [react(), tailwindcss()]
- server: { port: 5173, host: true }
- build: { target: 'es2022' }
- No WASM config needed (Phase 3 is pure TS)

**index.html:** Standard Vite HTML entry. `<html lang="fr">` (Budget Citoyen is a French app). Head: charset UTF-8, viewport meta (width=device-width, initial-scale=1.0), title "Budget Citoyen — Simulateur budgétaire". Body: `<div id="root"></div>` + `<script type="module" src="/src/main.tsx"></script>`. No external CSS or font links — Tailwind injects styles via Vite plugin, system font stack via index.css.

**src/main.tsx:** Per PATTERNS.md lines 413-437. Import StrictMode from react, createRoot from react-dom/client, App from ./App, and index.css. Guard: throw if root element not found. Render `<StrictMode><App /></StrictMode>`.

**src/index.css:** Per PATTERNS.md lines 1227-1238. First line: `@import "tailwindcss";`. Then `@theme { }` block containing ALL design tokens from UI-SPEC:
- --font-sans: System font stack from UI-SPEC line 24
- --color-* tokens: Copy all colors from UI-SPEC Color section (dominant #F8FAFC, secondary #F0F4F8, primary/accent #1E3A5F, destructive #DC2626, text colors, chart palette colors)
- --spacing-touch-target: 44px, --spacing-panel-width: 380px (Tailwind 4 arbitrary spacing)
- Use `--color-*` namespace syntax for all colors so Tailwind generates utilities like `bg-primary`, `text-text-primary`, etc.

**CRITICAL — CREATE A MINIMAL PLACEHOLDER `webapp/src/App.tsx`** so that `tsc --noEmit` passes after this task. The placeholder should export a simple component returning `<div>Budget Citoyen</div>`. This placeholder will be replaced in Plan 03-07.

Do NOT create data directory scaffolding — Plan 03-08 handles scenario data.
  </action>
  <verify>
    <automated>cd webapp && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>All 5 files created; tsc --noEmit exits 0; vite.config.ts uses Tailwind plugin; index.css defines all design tokens from UI-SPEC; main.tsx mounts App component with StrictMode</done>
</task>

<task type="auto">
  <name>Task 3: Create Vitest and Playwright test infrastructure configs</name>
  <files>webapp/vitest.config.ts, webapp/playwright.config.ts</files>
  <read_first>
webapp/vitest.config.ts — new file
webapp/playwright.config.ts — new file
webapp/vite.config.ts — created in Task 2 (for config merging in vitest)
@.planning/phases/03-interactive-simulation-shell-mvp/03-VALIDATION.md — lines 17-33 (test infrastructure requirements, sampling rates, quick run commands)
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md — lines 48-49 (vitest.config.ts and playwright.config.ts as new scaffolding)
  </read_first>
  <acceptance_criteria>
    <behavior>npx vitest run exits 0 (no tests yet — passes with "No test files found")</behavior>
    <behavior>npx playwright test exits 0 or 1 (no tests yet — exits with "No tests found" or empty suite pass)</behavior>
    <source>vitest.config.ts imports `defineConfig` from vitest and `mergeConfig` from vite</source>
    <source>vitest.config.ts sets `environment: 'jsdom'` and `include: ['src/**/*.{test,spec}.{ts,tsx}']`</source>
    <source>playwright.config.ts sets `testDir: './src/__tests__'` and `webServer.command: 'npx vite --port 5173'`</source>
    <source>playwright.config.ts includes `projects` for Chromium, Firefox, and WebKit (mobile: iPhone 13 viewport)</source>
    <cli>npx vitest run --config webapp/vitest.config.ts (exits 0, no tests)</cli>
  </acceptance_criteria>
  <action>
Create 2 test infrastructure files:

**vitest.config.ts:** Import defineConfig from vitest, mergeConfig from vite, and the viteConfig from ./vite.config.ts. Export default mergeConfig(viteConfig, defineConfig({ test: { environment: 'jsdom', include: ['src/**/*.{test,spec}.{ts,tsx}'], globals: false, css: true } })). This merges with the Vite config so plugins (React, Tailwind) are available during tests.

**playwright.config.ts:** Per VALIDATION.md lines 17-33. Export default defineConfig:
- testDir: './src/__tests__'
- fullyParallel: true
- forbidOnly: !!process.env.CI
- retries: process.env.CI ? 2 : 0
- workers: process.env.CI ? 1 : undefined
- reporter: 'html'
- use: baseURL 'http://localhost:5173', trace 'on-first-retry'
- webServer: command 'npx vite --port 5173', port 5173, reuseExistingServer !process.env.CI
- projects: Chromium (viewport 1280x720), Firefox (viewport 1280x720), WebKit (viewport 1280x720), Mobile Chrome (iPhone 13 viewport per VALIDATION.md UI-06), Mobile Safari (iPhone 13 viewport)

The Playwright config references axe-core via @axe-core/playwright imports in actual test files (not in config itself — axe-core is used at the test level).

Re-verify that `npx tsc --noEmit` still passes after these additions (the placeholder App.tsx from Task 2 handles the import reference).
  </action>
  <verify>
    <automated>cd webapp && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <done>vitest.config.ts exists with jsdom environment; playwright.config.ts exists with webServer config and 5 browser projects; both configs pass type-checking; npm run test exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network→Browser | Static assets (HTML/JS/CSS/JSON) served from CDN — no server-side computation |
| Main thread→Web Workers | postMessage with Transferable ArrayBuffers (Phase 2 workers) |
| User input→React state | Slider values, scenario selection, URL state decoding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Build tool dependencies (package.json) | mitigate | Pin major versions; npm audit on install; use `npm ci` in CI for deterministic installs |
| T-03-02 | Spoofing | vite.config.ts dev server host binding | mitigate | `host: true` for local dev only; production deployment uses static CDN serving — no dev server exposed |
| T-03-03 | Information Disclosure | Source maps in production build | mitigate | Vite defaults disable source maps in production; verify with `build.sourcemap: false` in vite.config.ts if needed |
| T-03-04 | Tampering | index.html CSP headers missing | accept | Phase 3 has no server-side routes; CSP headers are configured at CDN/deployment layer (Phase 5 hardening). No inline scripts in Phase 3 beyond Vite HMR (dev only) |
</threat_model>

<verification>
- [ ] `cd webapp && npm install` completes without errors
- [ ] `cd webapp && npx tsc --noEmit` exits 0
- [ ] `cd webapp && npx vitest run` exits 0 (no tests yet)
- [ ] `cd webapp && npx vite build` produces dist/ output
- [ ] All design tokens from UI-SPEC Color/Typography/Spacing sections present in index.css @theme
- [ ] package.json scripts block has dev, build, test, test:e2e
- [ ] placeholder App.tsx exists so tsc passes
</verification>

<success_criteria>
- webapp/ directory is an installable, buildable Vite+React+TS+Tailwind project
- All 41 npm packages declared (dependencies + devDependencies)
- Design tokens from UI-SPEC fully encoded in Tailwind @theme
- Test infrastructure ready for subsequent plans to add test files
- TypeScript strict mode enforced; no type errors on initial scaffold
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-simulation-shell-mvp/03-01-SUMMARY.md`
</output>
