---
phase: 03-interactive-simulation-shell-mvp
plan: 08
type: execute
wave: 5
depends_on: [03-07]
files_modified:
  - webapp/src/__tests__/simulator.spec.ts
  - webapp/src/__tests__/a11y.spec.ts
  - webapp/src/__tests__/components/LeverSlider.test.tsx
  - webapp/public/data/scenarios-v2025.1.json
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-05, A11Y-06]
must_haves:
  truths:
    - "Playwright E2E tests cover full user flow: load→splash→preselect→scenario→slider→reset→share"
    - "Axe-core Playwright tests return no accessibility violations across all states"
    - "LeverSlider component tests verify ARIA attributes via @testing-library/react"
    - "Minimal scenario data exists at public/data/scenarios-v2025.1.json for UI development"
  artifacts:
    - path: "webapp/src/__tests__/simulator.spec.ts"
      provides: "Playwright E2E tests covering UI-01 through UI-08"
      contains: "test.describe('Simulator E2E'"
    - path: "webapp/src/__tests__/a11y.spec.ts"
      provides: "Axe-core Playwright accessibility audit tests"
      contains: "AxeBuilder"
    - path: "webapp/src/__tests__/components/LeverSlider.test.tsx"
      provides: "Vitest component test with ARIA assertions"
      contains: "getByRole('slider')"
    - path: "webapp/public/data/scenarios-v2025.1.json"
      provides: "Minimal synthetic scenario data for UI development and tests"
      contains: "ScenarioDoc"
  key_links:
    - from: "webapp/src/__tests__/simulator.spec.ts"
      to: "webapp/src/pages/SimulatorPage.tsx"
      via: "Playwright page.goto('/')"
      pattern: "page.goto"
    - from: "webapp/src/__tests__/a11y.spec.ts"
      to: "@axe-core/playwright"
      via: "new AxeBuilder({ page }).analyze()"
      pattern: "AxeBuilder"
---

</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-CONTEXT.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-RESEARCH.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-UI-SPEC.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-VALIDATION.md
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md

<interfaces>
<!-- Key patterns for test files. -->

From VALIDATION.md (lines 39-54 — Per-Task Verification Map):
```
| Task ID | Requirement | Threat Ref | Test Type | Automated Command |
| 03-{plan}-01 | UI-01 | T-03-01 | e2e | playwright test tests/a11y.spec.ts:sliders |
| 03-{plan}-02 | UI-02 | — | unit+e2e | vitest run src/state/__tests__/interpolation.test.ts |
| 03-{plan}-03 | UI-03 | T-03-02 | e2e | playwright test tests/simulator.spec.ts:chart-render |
| ...etc for all 14 requirements
```

From VALIDATION.md (lines 62-69 — Wave 0 Requirements):
```
- vitest.config.ts (created in Plan 03-01)
- playwright.config.ts (created in Plan 03-01)
- src/state/__tests__/ (created in Plan 03-02)
- tests/simulator.spec.ts (created in THIS plan)
- tests/a11y.spec.ts (created in THIS plan)
- Framework install: already done in Plan 03-01
```

From PATTERNS.md Test File Patterns (lines 990-1067):
```
simulator.spec.ts: Playwright test.describe('Simulator E2E')
a11y.spec.ts: Playwright + AxeBuilder from @axe-core/playwright
LeverSlider.test.tsx: @testing-library/react + vitest
```

From engine/types.ts:
```typescript
export interface ScenarioDoc {
  definition: ScenarioDefinition;
  results: Record<string, ScenarioResult>;
}
export interface ScenarioDefinition {
  id: string; name: string; description: string;
  parameterOverrides: Record<string, number>;
}
```

From RESEARCH.md Risk Analysis (line 899):
```
"Phase 3 requires at least 9 scenario JSON files. Plan should include synthetic/minimal scenario generation task if CI pipeline isn't ready."
```

From CONTEXT.md D-03:
```
"9-12 pre-computed scenarios at launch (Baseline 2025 + 3-5 political programs + tax-only/full-program variants)."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate minimal synthetic scenario data for UI development</name>
  <files>webapp/public/data/scenarios-v2025.1.json</files>
  <read_first>
webapp/public/data/scenarios-v2025.1.json — new file (create public/data/ directory first)
webapp/src/engine/types.ts — ScenarioDoc and ScenarioDefinition interfaces (for correct JSON structure)
webapp/src/engine/scenario-cache.ts — ScenarioDoc interface (definition + results Record)
@.planning/phases/03-interactive-simulation-shell-mvp/03-CONTEXT.md — D-03 (9-12 pre-computed scenarios), D-06 (3 household profiles)
@.planning/phases/03-interactive-simulation-shell-mvp/03-RESEARCH.md — Open Question 1 (where scenario data comes from)
  </read_first>
  <acceptance_criteria>
    <behavior>Scenario JSON file loads without errors via ScenarioCache.loadFromJSON()</behavior>
    <behavior>At least 9 scenarios are defined with distinct ids, names, and descriptions</behavior>
    <behavior>Each scenario has pre-computed results for at least 3 profiles (indices 0, 1, 2)</behavior>
    <source>scenarios-v2025.1.json is valid JSON parseable by JSON.parse()</source>
    <source>JSON structure matches ScenarioDoc[] format: array of { definition: { id, name, description, parameterOverrides }, results: { "0": ScenarioResult, "1": ScenarioResult, "2": ScenarioResult } }</source>
    <source>Scenario ids include: "baseline-2025"</source>
    <source>Each ScenarioResult contains all 6 fields: ir, is, tva, cotisations, aides, revenuDisponible with realistic numeric values</source>
    <source>Scenario names and descriptions are in French (D-26 — niveau lycée)</source>
    <source>parameterOverrides map contains at least 5 keys matching LEVER_MAPPINGS subParams naming conventions</source>
    <source>Result values vary across scenarios (different tax regimes produce different household impacts — not all identical)</source>
    <source>File size is reasonable for static serving (under 500KB uncompressed for 9 scenarios × 3 profiles)</source>
    <source>Vite dev server serves the file at /data/scenarios-v2025.1.json (public/ directory is served as static root)</source>
    <cli>cd webapp && node -e "const fs = require('fs'); const data = JSON.parse(fs.readFileSync('public/data/scenarios-v2025.1.json','utf8')); console.log('Valid JSON:', data.length, 'scenarios');" 2>&1</cli>
  </acceptance_criteria>
  <action>
This task addresses RESEARCH.md Open Question 1 — scenario data availability. The CI pipeline from Phase 2 may not have produced real scenario data yet. Generate a minimal but structurally correct synthetic dataset for UI development and testing.

**Create directory:** `webapp/public/data/` (mkdir -p). The `public/` directory in Vite is served as static root — files at `public/data/scenarios-v2025.1.json` are accessible at `/data/scenarios-v2025.1.json`.

**JSON structure (ScenarioDoc[]):**
An array of 9 objects, each following the ScenarioDoc interface:
```
{ definition: { id, name, description, parameterOverrides }, results: { "0": ScenarioResult, "1": ScenarioResult, "2": ScenarioResult } }
```

**9 scenarios to define (D-03 — Baseline 2025 + political programs + variants):**

1. **baseline-2025** — "Budget 2025 — Loi de finances" — Situation de référence, tous les paramètres à leur valeur actuelle. parameterOverrides: {} (empty — baseline).
2. **reform-ir-baisse** — "Baisse de l'impôt sur le revenu" — Réduction de 15% des taux du barème IR. parameterOverrides: { 'ir.bareme.tranche1': -15, 'ir.bareme.tranche2': -15, ... } (all 5 brackets at -15).
3. **reform-ir-hausse** — "Hausse de l'impôt sur le revenu" — Augmentation de 15% des taux IR. All brackets at +15.
4. **reform-tva-baisse** — "Baisse de la TVA" — Réduction de 2 points du taux normal. parameterOverrides: { 'tva.taux.normal': -10, 'tva.taux.reduit': -5 }.
5. **reform-is-baisse** — "Baisse de l'impôt sur les sociétés" — parameterOverrides: { 'is.taux': -15 }.
6. **reform-cotisations-baisse** — "Baisse des cotisations sociales" — parameterOverrides: { 'cotisations.salariales': -10, 'cotisations.patronales': -10, 'cotisations.csg_crds': -5 }.
7. **reform-depenses-hausse** — "Hausse des dépenses publiques" — parameterOverrides: { 'depenses.spend_level': +10, 'depenses.effectifs': +5 }.
8. **programme-social** — "Programme social" — Combinaison: hausse IR (+10) + hausse dépenses (+15) + baisse TVA (-5). parameterOverrides blending multiple levers.
9. **programme-liberal** — "Programme libéral" — Combinaison: baisse IR (-10) + baisse IS (-15) + baisse dépenses (-10).

**ScenarioResult values (realistic ranges per profile):**
Generate distinct but plausible values showing that different tax regimes produce different household impacts:
- Profile 0 (modeste, SMIC): revenuDisponible ~1200-1800€, ir ~0-200€, tva ~100-300€, cotisations ~200-400€, aides ~200-500€
- Profile 1 (médian): revenuDisponible ~1800-2500€, ir ~100-500€, tva ~200-500€, cotisations ~400-800€, aides ~50-300€
- Profile 2 (aisé): revenuDisponible ~3000-6000€, ir ~500-2500€, tva ~400-1000€, cotisations ~800-1500€, aides ~0-100€
- revenuDisponible = base_income - ir - is - tva - cotisations + aides (ensure it makes arithmetic sense)
- Each scenario modifies these values proportionally to its parameter overrides (e.g., reform-ir-baisse reduces ir by ~15% for all profiles, increasing revenuDisponible)

All text in French, niveau lycée (D-26). scenario descriptions in active voice with "vous" address.

File size: 9 scenarios × 3 profiles × ~300 bytes/result ≈ ~8KB. Well under 500KB limit.

**Verification:** The generated JSON loads via `ScenarioCache.loadFromJSON('/data/scenarios-v2025.1.json')` without errors and produces 9 ScenarioDefinition entries with valid results for profiles 0, 1, 2.
  </action>
  <verify>
    <automated>cd webapp && node -e "const d=JSON.parse(require('fs').readFileSync('public/data/scenarios-v2025.1.json','utf8')); console.assert(d.length>=9,'Need >=9 scenarios, got '+d.length); d.forEach(s=>{console.assert(s.definition.id,'missing id'); console.assert(Object.keys(s.results).length>=3,'need >=3 profiles for '+s.definition.id)}); console.log('OK: '+d.length+' scenarios validated')"</automated>
  </verify>
  <done>scenarios-v2025.1.json contains 9 scenarios with 3 profiles each; valid JSON; served by Vite at /data/; ScenarioCache.loadFromJSON() loads successfully; realistic French names and descriptions</done>
</task>

<task type="auto">
  <name>Task 2: Create Playwright E2E and accessibility test suites</name>
  <files>webapp/src/__tests__/simulator.spec.ts, webapp/src/__tests__/a11y.spec.ts</files>
  <read_first>
webapp/src/__tests__/simulator.spec.ts — new file
webapp/src/__tests__/a11y.spec.ts — new file
webapp/playwright.config.ts — Playwright config created in Plan 03-01 (testDir, baseURL, projects)
@.planning/phases/03-interactive-simulation-shell-mvp/03-VALIDATION.md — lines 39-54 (per-task verification map with test commands), lines 62-69 (Wave 0 test file requirements)
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md — lines 990-1025 (simulator.spec.ts analog), lines 1030-1067 (a11y.spec.ts analog)
@.planning/phases/03-interactive-simulation-shell-mvp/03-UI-SPEC.md — lines 256-301 (State Machine for test flow design)
@.planning/phases/03-interactive-simulation-shell-mvp/03-CONTEXT.md — UI-01 through UI-08, A11Y-01 through A11Y-06 requirements
  </read_first>
  <acceptance_criteria>
    <behavior>E2E test: page loads splash screen, then transitions to preselect state with scenario cards visible</behavior>
    <behavior>E2E test: selecting a scenario displays household impact and enables sliders</behavior>
    <behavior>E2E test: slider drag updates impact values and triggers chart rendering</behavior>
    <behavior>E2E test: reset button clears selection and returns to preselect state</behavior>
    <behavior>E2E test: URL sharing round-trips (encode state, reload page, state restored)</behavior>
    <behavior>E2E test: methodology page renders with source attribution (Insee, budget.gouv.fr, Mésange)</behavior>
    <behavior>A11y test: axe-core finds no violations in preselect state</behavior>
    <behavior>A11y test: axe-core finds no violations after scenario selection with charts rendered</behavior>
    <behavior>A11y test: sliders have correct ARIA attributes</behavior>
    <source>simulator.spec.ts imports { test, expect } from '@playwright/test'</source>
    <source>simulator.spec.ts has test.describe blocks: 'Splash & Init', 'Scenario Selection', 'Slider Interaction', 'Reset', 'URL Sharing', 'Methodology Page', 'Mobile Layout'</source>
    <source>simulator.spec.ts test for splash screen: `await expect(page.getByText('Chargement du simulateur')).toBeVisible()`</source>
    <source>simulator.spec.ts test for preselect: `await expect(page.getByText('Choisissez un scénario')).toBeVisible()`</source>
    <source>simulator.spec.ts test for scenario selection: clicks scenario card, verifies impact pills appear, sliders become enabled</source>
    <source>simulator.spec.ts test for slider: uses keyboard (ArrowRight) on focused slider, verifies value changes</source>
    <source>simulator.spec.ts test for reset: clicks "Réinitialiser", verifies scenario deselected, sliders return to 0%</source>
    <source>simulator.spec.ts test for mobile: uses `page.setViewportSize({ width: 390, height: 844 })` (iPhone 14), verifies accordion layout</source>
    <source>simulator.spec.ts test for methodology: navigates to /methodologie, verifies text "Méthodologie", verifies "Insee" mentioned</source>
    <source>a11y.spec.ts imports AxeBuilder from '@axe-core/playwright'</source>
    <source>a11y.spec.ts test: `const results = await new AxeBuilder({ page }).analyze(); expect(results.violations).toEqual([])`</source>
    <source>a11y.spec.ts tests all 3 states: preselect (after splash), scenario displaying (after selection), error (simulate by blocking data fetch)</source>
    <source>a11y.spec.ts has targeted tests: slider ARIA attribute verification (A11Y-05), chart role="img" (A11Y-01), chart table th scope (A11Y-02)</source>
    <source>Both test files use async/await patterns and proper Playwright locators (getByText, getByRole, locator)</source>
    <cli>cd webapp && npx playwright test --list 2>&1 | head -20 (lists test files found)</cli>
  </acceptance_criteria>
  <action>
Create 2 Playwright test files following PATTERNS.md test conventions.

**simulator.spec.ts — E2E User Flow (UI-01 through UI-08):**

Per PATTERNS.md lines 990-1025 analog:
- Import { test, expect } from '@playwright/test'
- Test groups:
  1. **'Splash & Init':** page.goto('/') → expect splash text visible → wait for preselect transition (scenario card prompt) → timeout 30s for worker init
  2. **'Scenario Selection':** from preselect → click first scenario card → verify impact pills appear (text "Foyer modeste") → verify sliders no longer disabled
  3. **'Slider Interaction':** after scenario selected → focus first slider → keyboard ArrowRight → verify displayed value changes → verify chart grid appears
  4. **'Reset':** click "Réinitialiser" button → verify scenario deselected (no ring-2 on cards) → verify slider returns to "Actuel" 0% position
  5. **'URL Sharing':** select scenario → adjust slider → verify URL contains ?state= → reload page → verify state restored (same scenario selected, same slider position)
  6. **'Methodology':** page.goto('/methodologie') → verify heading "Méthodologie" → verify text contains "Insee", "budget.gouv.fr", "Mésange" → verify link back to simulator
  7. **'Mobile Layout':** set viewport iPhone 14 (390×844) → verify accordion "Réglages" exists → verify charts stack vertically (not 2×2 grid) → verify touch targets (buttons ≥ 44px via measurement or heuristic)

Use Playwright best practices:
- `page.waitForSelector()` for async transitions (splash→preselect, worker responses)
- `page.getByRole('slider')` for slider locators
- `page.getByText('Réinitialiser')` for button locators
- `expect().toBeVisible()` / `expect().toBeEnabled()` / `expect().toBeDisabled()` for state assertions
- `page.keyboard.press('ArrowRight')` for keyboard navigation tests

**a11y.spec.ts — Accessibility Audit (A11Y-01 through A11Y-06):**

Per PATTERNS.md lines 1030-1067 analog:
- Import { test, expect } from '@playwright/test' and AxeBuilder from '@axe-core/playwright'
- Test groups:
  1. **'Preselect state — no violations':** navigate to / → wait for preselect → run axe → expect(results.violations).toEqual([])
  2. **'Scenario displaying — no violations':** select scenario → wait for charts → run axe → expect no violations
  3. **'Slider ARIA (A11Y-05)':** verify slider has aria-valuenow, aria-valuemin='-30', aria-valuemax='30' attributes
  4. **'Chart ARIA (A11Y-01)':** verify chart SVG containers have role="img" attribute
  5. **'Chart aria-labelledby (A11Y-01)':** verify chart figures have aria-labelledby pointing to a visible figcaption
  6. **'Chart table fallback (A11Y-02)':** verify each chart has a sibling table element with th[scope="col"]
  7. **'Pattern fills (A11Y-03)':** verify SVG `<pattern>` elements exist in the chart defs
  8. **'Deuteranopia-safe colors (A11Y-03)':** verify chart palette (or check pattern+color used, not color alone)
  9. **'Slider keyboard nav (A11Y-05)':** focus slider → ArrowRight → verify aria-valuenow updated

Use `AxeBuilder({ page }).analyze()` for comprehensive checks. Use `AxeBuilder({ page }).include('#simulator-root')` to scope to the app. Document any known false positives with `AxeBuilder({ page }).disableRules(['rule-id'])`.

All test files follow Playwright conventions (not Phase 2 TS conventions). Use async/await throughout.
  </action>
  <verify>
    <automated>cd webapp && npx playwright test --list 2>&1</automated>
  </verify>
  <done>simulator.spec.ts covers full E2E flow: splash→preselect→scenario→slider→reset→URL→methodology→mobile; a11y.spec.ts runs axe-core on all states with ARIA assertion tests; all 14 requirements addressed</done>
</task>

<task type="auto">
  <name>Task 3: Create LeverSlider component test with ARIA assertions</name>
  <files>webapp/src/__tests__/components/LeverSlider.test.tsx</files>
  <read_first>
webapp/src/__tests__/components/LeverSlider.test.tsx — new file (create __tests__/components/ directory)
webapp/src/components/LeverSlider.tsx — created in Plan 03-05 (to understand props and rendered output)
@.planning/phases/03-interactive-simulation-shell-mvp/03-PATTERNS.md — lines 948-986 (LeverSlider.test.tsx analog — exact pattern to follow)
@.planning/phases/03-interactive-simulation-shell-mvp/03-UI-SPEC.md — lines 140-154 (Slider contract with aria-valuenow/min/max values)
  </read_first>
  <acceptance_criteria>
    <behavior>Component test: LeverSlider renders with correct ARIA attributes (aria-valuenow, aria-valuemin, aria-valuemax)</behavior>
    <behavior>Component test: keyboard navigation fires onChange callback</behavior>
    <behavior>Component test: "Actuel" label visible at baseline position</behavior>
    <source>LeverSlider.test.tsx imports { render, screen } from '@testing-library/react'</source>
    <source>LeverSlider.test.tsx imports userEvent from '@testing-library/user-event'</source>
    <source>LeverSlider.test.tsx imports { describe, it, expect, vi } from 'vitest'</source>
    <source>Test renders LeverSlider with minValue=-30, maxValue=30, step=1, label="IR ménages"</source>
    <source>Test asserts slider has aria-valuemin='-30', aria-valuemax='30', aria-valuenow='0' (default)</source>
    <source>Test asserts "Actuel" text is in the document</source>
    <source>Test simulates keyboard ArrowRight → asserts onValueChange called with value 1</source>
    <source>Test simulates keyboard ArrowRight×5 → asserts onValueChange called 5 times</source>
    <source>Test renders disabled slider → asserts aria-disabled or not-allowed state</source>
    <cli>cd webapp && npx vitest run src/__tests__/components/LeverSlider.test.tsx 2>&1</cli>
  </acceptance_criteria>
  <action>
Per PATTERNS.md lines 948-986 analog — create a Vitest component test for LeverSlider using @testing-library/react.

**Dependencies needed:** `@testing-library/react` and `@testing-library/user-event` for React component testing with Vitest. These should already be available (they're standard for React testing) — if not, install as devDependencies: `npm install -D @testing-library/react @testing-library/user-event @testing-library/jest-dom`.

**Test structure (following PATTERNS.md Vitest conventions):**

```
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { LeverSlider } from '../../components/LeverSlider';

describe('LeverSlider', () => {
  // ARIA attributes test
  it('renders with correct ARIA attributes', () => { ... });
  
  // "Actuel" label test
  it('displays "Actuel" label at baseline position', () => { ... });
  
  // Keyboard navigation test (A11Y-05)
  it('calls onValueChange with keyboard ArrowRight', async () => { ... });
  
  // Multiple key presses
  it('accumulates keyboard changes correctly', async () => { ... });
  
  // Disabled state test
  it('does not respond to keyboard when disabled', async () => { ... });
  
  // Custom label test
  it('displays the provided label text', () => { ... });
});
```

Use `render(<LeverSlider ... />)`, then `screen.getByRole('slider')` for ARIA assertions. Use `userEvent.setup()` for keyboard simulation. Use `vi.fn()` for callback mocks.

Add the test to the vitest config include pattern (already set to `src/**/*.{test,spec}.{ts,tsx}` in Plan 03-01 — this test will be auto-discovered).

**Test environment note:** LeverSlider uses React Aria hooks which require a browser-like environment. jsdom (configured in vitest.config.ts in Plan 03-01) supports most ARIA attributes and DOM interactions. If React Aria's `useSliderState` throws in jsdom, mock `useSliderState` from `react-stately` at the top of the test file to return a controlled state object.

File conventions: banner comment, section dividers, describe/it pattern from Phase 2 test conventions.
  </action>
  <verify>
    <automated>cd webapp && npx vitest run src/__tests__/components/LeverSlider.test.tsx 2>&1</automated>
  </verify>
  <done>LeverSlider.test.tsx passes: ARIA attributes verified, keyboard navigation triggers callbacks, "Actuel" label present, disabled state prevents interaction</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario JSON data → app state | Static JSON file served from CDN — data is pre-computed, no user injection |
| Test assertions → DOM state | Playwright/Vitest tests verify UI behavior — no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Tampering | Test data injection via scenario JSON | accept | scenarios-v2025.1.json is a development/test fixture. Production scenarios come from CI pipeline with integrity hashes. Test fixture is synthetic — any tampering only affects test results, not user data |
| T-03-20 | Information Disclosure | Test snapshots containing fixture data | accept | Test data is synthetic (not real citizen data). No PII in scenario fixtures. Acceptable for open-source project |
</threat_model>

<verification>
- [ ] scenarios-v2025.1.json exists at webapp/public/data/ with 9+ scenarios × 3 profiles
- [ ] JSON structure matches ScenarioDoc[] interface
- [ ] simulator.spec.ts covers splash→preselect→scenario→slider→reset→URL→methodology→mobile
- [ ] a11y.spec.ts runs axe-core on preselect and scenario_displaying states
- [ ] a11y.spec.ts has targeted ARIA tests (slider, chart, table)
- [ ] LeverSlider.test.tsx passes with ARIA assertions
- [ ] `npm run test` (vitest run) passes
- [ ] `npm run test:e2e` (playwright test) discovers test files
</verification>

<success_criteria>
- Minimal synthetic scenario data available for UI development (9 scenarios × 3 profiles)
- Playwright E2E tests cover the complete citizen user flow
- Axe-core Playwright tests verify zero accessibility violations in all states
- LeverSlider component test validates ARIA attributes and keyboard navigation
- All 14 phase requirements (UI-01 through A11Y-06) have at least one test covering them
- VALIDATION.md Wave 0 items all cleared (all test files created)
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-simulation-shell-mvp/03-08-SUMMARY.md`
</output>
