---
phase: 02-core-simulation-engines-wasm
plan: 11
type: execute
wave: 7
depends_on: ["02-09", "02-10"]
gap_closure: true
closes_gaps:
  - "5 new-architecture files missing: scenario_definitions.py, precompute.py, scenarios-v2025.1.json, scenario_types.rs (now TS), cache.rs (now TS)"
  - "Summary/Plan mismatch: 02-04, 02-06, 02-07, 02-08 SUMMARYs document old architecture"
  - "REQUIREMENTS.md has false completion markers for MICRO-01/02/03/05"
  - "Performance criteria unverifiable (SC-4) — resolved by TS benchmarks"
  - "CI workflow never executed — no CI evidence of passing gates (SC-5)"
files_modified:
  - packages/data-pipeline/src/scenarios/__init__.py
  - packages/data-pipeline/src/scenarios/scenario_definitions.py
  - packages/data-pipeline/src/scenarios/precompute.py
  - packages/data-pipeline/dist/scenarios-v2025.1.json
  - packages/data-pipeline/pyproject.toml
  - .github/workflows/phase2-wasm.yml
  - .planning/phases/02-core-simulation-engines-wasm/02-04-SUMMARY.md
  - .planning/phases/02-core-simulation-engines-wasm/02-06-SUMMARY.md
  - .planning/phases/02-core-simulation-engines-wasm/02-07-SUMMARY.md
  - .planning/phases/02-core-simulation-engines-wasm/02-08-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - MICRO-01
  - MICRO-02
  - MICRO-03
  - MICRO-04
  - MICRO-05
  - MACRO-04

must_haves:
  truths:
    - "Python scenario pre-compute pipeline generates scenarios-v2025.1.json using openfisca-france against canonical profiles"
    - "At least 3 candidate scenarios defined with distinct reform parameter sets (status quo, expansion, consolidation)"
    - "CI workflow updated to include scenario pre-compute step and TypeScript engine tests (vitest)"
    - "Scenario pre-compute runs in CI and fails build if openfisca-france version mismatches"
    - "REQUIREMENTS.md updated: MICRO-01/02/03 marked as satisfied via pre-computation (not client-side formula engine)"
    - "All 4 SUMMARY files (02-04, 02-06, 02-07, 02-08) rewritten to reflect the hybrid/simplified TypeScript architecture"
    - "ROADMAP.md Phase 2 success criteria updated to match simplified architecture"
    - "CI workflow has at least one successful execution (or dry-run validation)"
  artifacts:
    - path: "packages/data-pipeline/src/scenarios/scenario_definitions.py"
      provides: "3+ candidate reform scenario parameter sets using openfisca-france reform API"
      contains: "def get_scenario_definitions"
    - path: "packages/data-pipeline/src/scenarios/precompute.py"
      provides: "CI pipeline: runs openfisca-france for each scenario × canonical profile, exports structured JSON"
      contains: "def precompute_scenarios"
    - path: "packages/data-pipeline/dist/scenarios-v2025.1.json"
      provides: "Versioned compressed static file with scenario definitions + pre-computed ScenarioResult for all profiles"
      contains: "scenarios-v2025.1"
    - path: ".github/workflows/phase2-wasm.yml"
      provides: "Updated CI: scenario pre-compute + TypeScript vitest tests + core cargo test"
      contains: "vitest"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated with correct MICRO-* completion markers reflecting hybrid architecture"
      contains: "MICRO-01"
  key_links:
    - from: "packages/data-pipeline/src/scenarios/precompute.py"
      to: "openfisca_france.FranceTaxBenefitSystem"
      via: "Python OpenFisca compute API"
      pattern: "FranceTaxBenefitSystem"
    - from: "packages/data-pipeline/dist/scenarios-v2025.1.json"
      to: "webapp/src/engine/scenario-cache.ts"
      via: "Browser fetch + decompress + ScenarioCache.loadFromJSON()"
      pattern: "scenarios-v2025.1"
    - from: "packages/data-pipeline/src/scenarios/precompute.py"
      to: "packages/data-pipeline/dist/bilingual_test_fixtures.json"
      via: "Canonical profiles as input to scenario compute"
      pattern: "test_fixtures"
---

<objective>
Complete the hybrid architecture: implement the Python scenario pre-compute pipeline using openfisca-france, generate the versioned scenario data file, update CI to execute the pipeline and run TypeScript tests, and rewrite all documentation (SUMMARYs, REQUIREMENTS.md, ROADMAP.md) to reflect the simplified pure-TypeScript + Python backend architecture.
</objective>

<tasks>
<task id="11-01" name="Implement Python scenario pre-compute pipeline" rule_category="Addition">
<description>
Create the scenario pipeline that replaces the deprecated codegen:

1. `packages/data-pipeline/src/scenarios/scenario_definitions.py`:
   - Define at least 3 candidate scenarios using openfisca-france reform API
   - Status quo: current law (no parameter changes)
   - Fiscal expansion: reduced IR + increased aides (parameter overrides)
   - Fiscal consolidation: increased TVA + reduced spending (parameter overrides)
   - Each scenario: id, name, description, parameter_overrides dict
   - Export: `get_scenario_definitions()` → list of ScenarioDefinition

2. `packages/data-pipeline/src/scenarios/precompute.py`:
   - `precompute_scenarios(scenarios, profiles)` → ScenarioDoc
   - For each scenario × canonical profile:
     a. Create Simulation with scenario's parameter overrides applied
     b. Compute IR, IS, TVA, cotisations, aides sociales, revenu_disponible
     c. Collect into ScenarioResult per profile
   - Export to `packages/data-pipeline/dist/scenarios-v2025.1.json`
   - Version tag in filename and metadata
   - Compression: gzip or brotli for small file size (<500KB per D-22)

3. Update `packages/data-pipeline/pyproject.toml`:
   - Add scenario pre-compute script entry point
   - Add openfisca-france dependency (if not already present)
   - Remove codegen pipeline references
</description>
</task>

<task id="11-02" name="Update CI workflow for simplified architecture" rule_category="Mutation">
<description>
Rewrite `.github/workflows/phase2-wasm.yml` (rename to phase2-engines.yml):

1. Remove all wasm-pack jobs (wasm-pack build, wasm-pack test --headless)
2. Remove WASM crate test jobs (budget-citoyen-wasm-micro, budget-citoyen-wasm-macro)
3. Keep core crate tests: `cargo test -p budget-citoyen-core`
4. Add scenario pre-compute job: runs `python -m packages.data-pipeline.src.scenarios.precompute`
5. Add TypeScript engine test job: `npm run test` (vitest) in webapp/
6. Add version consistency gate: validates scenarios-v2025.1 version against openfisca-france version
7. Add OpenFisca staleness check: validates parameter files haven't changed since last pre-compute
8. Add macro interpolation validation: TS output vs reference Rust output comparison
</description>
</task>

<task id="11-03" name="Rewrite SUMMARY files and update documentation" rule_category="Documentation">
<description>
Rewrite the 4 SUMMARY files that document the old architecture:

1. `02-04-SUMMARY.md`: Document scenario data format and pre-compute pipeline (not codegen)
2. `02-06-SUMMARY.md`: Document scenario cache and lookup engine (not TaxBenefitSystem)
3. `02-07-SUMMARY.md`: Document TypeScript engines replacing WASM boundary (not MicroEngine WASM exports)
4. `02-08-SUMMARY.md`: Document simplified workers and CI (not WASM worker imports)

Update project documentation:
5. `REQUIREMENTS.md`: Fix MICRO-01/02/03/05 markers. MICRO-01/02/03 = satisfied via Python pre-computation. MICRO-04 = satisfied (zero citizen data transfer). MICRO-05 = satisfied (O(1) lookup < 1ms). Add note explaining the hybrid architecture.
6. `ROADMAP.md`: Update Phase 2 success criteria to reflect pure TS + Python architecture. Remove WASM-specific criteria.
</description>
</task>

<task id="11-04" name="Verify end-to-end pipeline" rule_category="Verification">
<description>
1. Run scenario pre-compute pipeline: `python -m packages.data-pipeline.src.scenarios.precompute`
2. Verify scenarios-v2025.1.json exists and contains all 3+ scenarios × canonical profiles
3. Verify file size < 500KB (gzip'd)
4. Run CI workflow locally (act or dry-run): `cargo test -p budget-citoyen-core`
5. Run TypeScript engine tests: vitest
6. Verify SUMMARY files have no references to wasm-pack, wasm-bindgen, generated/, TaxBenefitSystem, MicroEngine
7. Verify REQUIREMENTS.md markers are correct
</description>
</task>
</tasks>
</parameter>
