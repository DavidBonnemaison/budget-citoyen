---
phase: 02-core-simulation-engines-wasm
plan: 09
type: execute
wave: 6
depends_on: ["02-08"]
gap_closure: true
closes_gaps:
  - "All 340+ generated formula functions are still 0.0_f64 stubs — no real micro computation (SC-1)"
  - "Architectural drift: 4 plans revised to hybrid architecture but codebase unchanged — 5 new-architecture files missing"
  - "Summary/Plan mismatch: 02-04, 02-06, 02-07, 02-08 SUMMARYs document old architecture, PLANs document new"
files_modified:
  - Cargo.toml
  - packages/core/Cargo.toml
  - packages/wasm-micro/
  - packages/wasm-macro/
  - packages/wasm-micro/src/
  - packages/wasm-macro/src/
  - packages/wasm-micro/tests/
  - packages/wasm-macro/tests/
  - .github/workflows/phase2-wasm.yml
  - packages/data-pipeline/src/codegen/
  - webapp/src/workers/micro-worker.ts
  - webapp/src/workers/macro-worker.ts
autonomous: true
requirements:
  - MICRO-04
  - MICRO-05

must_haves:
  truths:
    - "All 340+ generated formula functions (3,336 lines in wasm-micro/src/generated/) are deleted — no stub code remains"
    - "The wasm-micro Rust crate is removed entirely — TaxBenefitSystem, SimulationState, MicroEngine, and generated/ stubs are all gone"
    - "The wasm-macro Rust crate is removed entirely — interpolation logic moves to TypeScript in Plan 02-10"
    - "All WASM-related dependencies removed from workspace Cargo.toml (wasm-bindgen, serde-wasm-bindgen, interpn, ndarray, postcard, console_error_panic_hook)"
    - "Cargo workspace updated to remove wasm-micro and wasm-macro members"
    - "CI workflow removes wasm-pack steps and WASM crate tests"
    - "Old TypeScript workers (micro-worker.ts, macro-worker.ts) removed — rebuilt in Plan 02-10"
    - "Codegen pipeline (packages/data-pipeline/src/codegen/) removed"
    - "Zero cargo check/test failures after crate removal"
  artifacts:
    - path: "packages/core/Cargo.toml"
      provides: "Updated workspace member list (core only)"
      contains: "[workspace]"
    - path: "Cargo.toml"
      provides: "Workspace root with only core crate"
      contains: "members = ["
    - path: ".github/workflows/phase2-wasm.yml"
      provides: "Updated CI removing wasm-pack and WASM crate test jobs"
      contains: "wasm-pack"
  key_links:
    - from: "packages/core/Cargo.toml"
      to: "budget-citoyen-core"
      via: "Cargo workspace dependency"
      pattern: "budget-citoyen-core"
---

<objective>
Remove all traces of the old WASM-based architecture. Delete the wasm-micro crate (3,800 lines including generated/formula stubs, TaxBenefitSystem, SimulationState, MicroEngine), delete the wasm-macro crate (540 lines, interpolation logic moves to TypeScript in Plan 02-10), remove all WASM-specific Rust dependencies, clean up the Python codegen pipeline, remove old TypeScript workers, and update the CI workflow.
</objective>

<tasks>
<task id="09-01" name="Remove wasm-micro and wasm-macro Rust crates" rule_category="Mutation">
<description>
Delete both WASM crates entirely:
1. Remove `packages/wasm-micro/` directory (all .rs files, Cargo.toml, tests/, pkg/)
2. Remove `packages/wasm-macro/` directory (all .rs files, Cargo.toml, tests/, pkg/)
3. Remove both crate members from workspace `Cargo.toml`
4. Run `cargo check --workspace` to verify clean removal
5. Run `cargo test -p budget-citoyen-core` to verify core tests still pass

Also delete the Python codegen pipeline:
6. Remove `packages/data-pipeline/src/codegen/` directory
7. Remove any codegen references from pyproject.toml
</description>
</task>

<task id="09-02" name="Remove WASM dependencies and update CI" rule_category="Mutation">
<description>
Clean up all WASM-specific dependencies and tooling:
1. Remove WASM dependencies from workspace Cargo.toml (strip wasm-bindgen, serde-wasm-bindgen, interpn, ndarray, postcard, console_error_panic_hook if present at workspace level)
2. Remove wasm-pack test jobs from CI workflow (.github/workflows/phase2-wasm.yml)
3. Remove old TypeScript workers: `webapp/src/workers/micro-worker.ts` and `webapp/src/workers/macro-worker.ts` (rebuilt in Plan 02-10)
4. Verify `cargo check --workspace` passes
5. Verify `cargo test -p budget-citoyen-core` passes
</description>
</task>

<task id="09-03" name="Verify and commit" rule_category="Verification">
<description>
Final verification:
1. Confirm zero references to wasm-micro or wasm-macro in Cargo.toml files
2. Confirm zero references to wasm-bindgen in any Cargo.toml
3. Confirm `packages/wasm-micro/` and `packages/wasm-macro/` directories do not exist
4. Confirm `packages/data-pipeline/src/codegen/` directory does not exist
5. Confirm core tests pass
6. Run workspace-wide cargo check
</description>
</task>
</tasks>
</parameter>
