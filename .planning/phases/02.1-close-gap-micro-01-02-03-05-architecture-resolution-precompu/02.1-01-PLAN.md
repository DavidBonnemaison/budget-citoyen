---
phase: 02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/data-pipeline/src/scenarios/scenario_definitions.py
  - packages/data-pipeline/src/scenarios/precompute.py
  - packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json
autonomous: true
requirements:
  - MICRO-01
  - MICRO-02
  - MICRO-03

must_haves:
  truths:
    - "12 ScenarioDefinition instances exist in get_scenario_definitions() return list"
    - "Baseline scenario has empty parameter_overrides"
    - "5 political program scenarios have IR bareme + social benefit parameter overrides matching publicly stated platforms"
    - "3 pedagogical scenarios use extended override keys (cotisations.scale, revenu_universel.montant_mensuel)"
    - "3 single-axis variants use symmetric ± values for IR, TVA, and spending"
    - "Scenarios with cotisation overrides produce distinct cotisation results from baseline"
    - "Output JSON is structurally valid with all 12 scenarios and required fields present"
    - "IS result is always 0.0 (per D-04)"
  artifacts:
    - path: "packages/data-pipeline/src/scenarios/scenario_definitions.py"
      provides: "12 ScenarioDefinition instances (3 existing + 9 new)"
      min_lines: 400
    - path: "packages/data-pipeline/src/scenarios/precompute.py"
      provides: "Extended override extraction including cotisations.scale and revenu_universel"
      contains: "cotisation_scale"
    - path: "packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json"
      provides: "JSON Schema (Draft 2020-12) for scenarios-v2025.1.json output"
      contains: "minItems"
  key_links:
    - from: "get_scenario_definitions() return list"
      to: "precompute_scenarios() loop"
      via: "len(scenarios) >= 12"
      pattern: "len.*scenario.*12"
    - from: "override extraction block"
      to: "_estimate_cotisations(scale=cotisation_scale)"
      via: "cotisation_scale variable"
      pattern: "cotisation_scale"
    - from: "override extraction block"
      to: "_estimate_aides() revenu_universel logic"
      via: "ru_montant and ru_remplace_aides variables"
      pattern: "revenu_universel"
    - from: "scenarios.schema.json"
      to: "scenarios-v2025.1.json"
      via: "jsonschema Draft202012Validator in CI"
      pattern: "Draft202012Validator"
---

<objective>
Expand the scenario definition catalog from 3 to 12 scenarios (per D-01), extend the pre-compute pipeline with two new recognized override keys for pedagogical scenarios (cotisations scale, revenu universel), and create the JSON Schema contract (Draft 2020-12) for output validation (per D-08).

Purpose: The scenario definitions are the input to the pre-compute pipeline. Without 12 scenarios covering the interpolation grid, Phase 3 cannot render per-tax interpolation. The JSON Schema is the gate that prevents malformed output from reaching the TypeScript consumer.
Output: 12 ready-to-compute ScenarioDefinition instances, extended override extraction logic, and a validated JSON Schema file.
</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-CONTEXT.md
@.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md
@.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/data-pipeline/src/scenarios/scenario_definitions.py:
```python
@dataclass
class ScenarioDefinition:
    id: str
    name: str
    description: str
    parameter_overrides: Dict[str, Any] = field(default_factory=dict)
    _reform_class: Optional[Type[Reform]] = field(default=None, repr=False)

    def build_reform(self):
        """Lazy-imports openfisca-france, constructs Reform subclass."""

def get_scenario_definitions() -> List[ScenarioDefinition]:
    """Returns the list of candidate reform scenario definitions."""
```

From packages/data-pipeline/src/scenarios/precompute.py:
```python
def _compute_scenario_result(
    profile: Dict[str, Any],
    scenario: ScenarioDefinition,
    profile_index: int,
) -> Dict[str, float]:
    """Returns: {"ir": float, "is": 0.0, "tva": float, "cotisations": float, "aides": float, "revenuDisponible": float}"""

# Override extraction keys (lines 318-353):
# "impot_revenu.bareme"              → ir_scale (float)
# "prestations_sociales.rsa.socle"   → aide_scales["rsa"] (float)
# "prestations_sociales.prime_activite.montant_base" → aide_scales["prime_activite"] (float)
# "prestations_sociales.allocations_familiales.modulation" → aide_scales["allocations_familiales"] (float)
# "prestations_sociales.apl.revalorisation" → aide_scales["apl"] (1.0 if 0.0)
# "tva.taux_normal"                  → taux_tva (float 0.0-1.0)
```

From webapp/src/engine/types.ts:
```typescript
export interface ScenarioDefinition {
  id: string;
  name: string;
  description: string;
  parameterOverrides: Record<string, number>;
}
export interface ScenarioResult {
  ir: number; is: number; tva: number; cotisations: number; aides: number; revenuDisponible: number;
}
```

From packages/data-pipeline/src/schemas/parameter.schema.json (schema convention):
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://budget-citoyen.fr/schemas/...",
  "title": "...",
  "description": "...",
  "type": "object",
  "required": [...],
  "properties": { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON Schema for scenarios-v2025.1.json output document</name>
  <files>packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json</files>
  <read_first>
    packages/data-pipeline/src/schemas/parameter.schema.json — the schema convention to replicate (Draft 2020-12, $id namespace, required/properties structure)
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md: lines 359-418 — the full schema template to copy verbatim
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-PATTERNS.md: lines 23-66 — schema structure patterns and conventions
    webapp/src/engine/types.ts: lines 46-77 — ScenarioResult and ScenarioDefinition TypeScript interfaces that define the contract
  </read_first>
  <action>
    Create file: packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json

    Copy the schema template from RESEARCH.md lines 359-418 verbatim with these exact conventions from parameter.schema.json:
    - $schema: "https://json-schema.org/draft/2020-12/schema"
    - $id: "https://budget-citoyen.fr/schemas/scenarios-v2025.1.schema.json"
    - Top-level type: "object", required: ["scenarios", "metadata"]

    Schema structure to enforce:
    - scenarios: array, minItems: 12
      - Each item: object, required: ["definition", "results"]
        - definition: object, required: ["id", "name", "description", "parameterOverrides"]
          - id: string, pattern: "^[a-z0-9-]+$", minLength: 3
          - name: string, minLength: 5
          - description: string, minLength: 20
          - parameterOverrides: object
        - results: object, minProperties: 32, additionalProperties schema:
          - required: ["ir", "is", "tva", "cotisations", "aides", "revenuDisponible"]
          - ir: number
          - is: number, const: 0.0
          - tva: number, minimum: 0
          - cotisations: number, minimum: 0
          - aides: number, minimum: 0
          - revenuDisponible: number, minimum: 0
    - metadata: object, required: ["version", "reference_year", "generated_at", "total_scenarios", "total_profiles"]
      - version: string, const: "v2025.1"
      - reference_year: integer, const: 2025
      - generated_at: string, format: "date-time"
      - openfisca_france_version: string
      - total_scenarios: integer, minimum: 12
      - total_profiles: integer, const: 32

    Create the schemas/ directory if it does not exist: packages/data-pipeline/src/scenarios/schemas/
  </action>
  <acceptance_criteria>
    - File exists at packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json
    - File contains "$schema": "https://json-schema.org/draft/2020-12/schema"
    - File contains "minItems": 12
    - File contains "const": 0.0 for is field
    - File contains "const": "v2025.1" for version
    - File contains "const": 32 for total_profiles
    - File contains "minProperties": 32 for results
    - python3 -c "import json; json.load(open('packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json'))" exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import json
from jsonschema import Draft202012Validator, SchemaError
with open('packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json') as f:
    schema = json.load(f)
Draft202012Validator.check_schema(schema)
print('Schema is valid Draft 2020-12')
"</automated>
  </verify>
  <done>JSON Schema file created, valid Draft 2020-12, matching the Schema convention from parameter.schema.json</done>
</task>

<task type="auto">
  <name>Task 2: Expand scenario_definitions.py from 3 to 12 scenarios</name>
  <files>packages/data-pipeline/src/scenarios/scenario_definitions.py</files>
  <read_first>
    packages/data-pipeline/src/scenarios/scenario_definitions.py — current 3 scenarios, get_scenario_definitions() factory, dataclass + build_reform() pattern
    packages/data-pipeline/src/scenarios/precompute.py: lines 278-404 — _compute_scenario_result() showing recognized override keys (impot_revenu.bareme, prestations_sociales.rsa.socle, etc.)
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md: lines 249-269 — recognized override keys table and new key definitions
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-PATTERNS.md: lines 197-326 — scenario definition patterns and 9 new scenario descriptions
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-CONTEXT.md: lines 14-44 — D-01 through D-05 scenario scope decisions
  </read_first>
  <action>
    Modify file: packages/data-pipeline/src/scenarios/scenario_definitions.py

    Expand get_scenario_definitions() from 3 to 12 ScenarioDefinition instances.
    Preserve the 3 existing scenarios (baseline-2025, expansion-2025, consolidation-2025) unchanged.
    Add 9 new ScenarioDefinition entries after the existing 3. Follow the exact same docstring, @dataclass field, and block-comment pattern.

    The 9 new scenarios, in order:

    ── Political Programs (5) ──

    1. id="lfi-nfp-2025"
       name="LFI/NFP — Programme fiscal 2027"
       description: "Proposition LFI/NFP : refonte du barème IR (14 tranches, taux marginal 90% au-delà de 400K€ pour les couples), hausse du SMIC à 1600€ net, rétablissement d'un ISF climatique, sortie des traités de libre-échange. Transferts sociaux renforcés — RSA socle +30%, prime d'activité augmentée de 50%, APL revalorisées. Planification écologique financée par l'emprunt et la taxation du capital."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.01, "threshold": 5000}, {"rate": 0.05, "threshold": 10000},
           {"rate": 0.10, "threshold": 15000}, {"rate": 0.15, "threshold": 25000}, {"rate": 0.20, "threshold": 40000},
           {"rate": 0.30, "threshold": 60000}, {"rate": 0.40, "threshold": 90000}, {"rate": 0.50, "threshold": 130000},
           {"rate": 0.60, "threshold": 180000}, {"rate": 0.70, "threshold": 250000}, {"rate": 0.80, "threshold": 325000},
           {"rate": 0.85, "threshold": 400000}, {"rate": 0.90, "threshold": 500000}
         ]}
         "prestations_sociales.rsa.socle": 1.30
         "prestations_sociales.prime_activite.montant_base": 1.50
         "prestations_sociales.apl.revalorisation": 1.0

    2. id="renaissance-2025"
       name="Renaissance — Simplification fiscale 2027"
       description: "Proposition Renaissance : simplification du barème IR (4 tranches au lieu de 5), fusion de la CSG et de l'IR dans un prélèvement unique, suppression de la CVAE résiduelle, maintien du crédit d'impôt recherche. Baisse des cotisations sociales salariales de 2 points pour les salaires jusqu'à 2,5 SMIC. Prime d'activité revalorisée de 15% pour les travailleurs à bas revenus. Stabilité globale de la pression fiscale — recentrage sur l'activité et l'innovation."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.11, "threshold": 12000},
           {"rate": 0.30, "threshold": 50000}, {"rate": 0.45, "threshold": 180000}
         ]}
         "prestations_sociales.prime_activite.montant_base": 1.15
         "cotisations.scale": 0.90

    3. id="lr-2025"
       name="LR — Retour à l'équilibre budgétaire"
       description: "Proposition LR : baisse de 10% des dépenses publiques hors régaliens, réduction du nombre de fonctionnaires (non-remplacement d'un départ sur deux), report de l'âge légal de départ à 65 ans. Baisse de l'impôt sur les sociétés (taux normal à 20%), suppression de l'IFI (remplacé par un abattement renforcé sur la résidence principale). Allocations familiales réservées aux ménages sous 60K€ de revenus. TVA augmentée à 22% pour financer la transition énergétique."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.10, "threshold": 12000},
           {"rate": 0.27, "threshold": 30000}, {"rate": 0.37, "threshold": 85000},
           {"rate": 0.40, "threshold": 180000}
         ]}
         "prestations_sociales.allocations_familiales.modulation": 0.50
         "prestations_sociales.apl.revalorisation": 0.0
         "tva.taux_normal": 0.22
         "cotisations.scale": 1.0

    4. id="rn-2025"
       name="RN — Pouvoir d'achat & priorité nationale"
       description: "Proposition RN : baisse de la TVA sur les produits de première nécessité (taux réduit à 2,1% sur 100 produits essentiels), baisse de l'IR pour les ménages modestes (rehaussement des seuils de 15%), suppression de l'AME (Aide Médicale d'État). Baisse des cotisations sociales sur les bas salaires (exonération charges patronales jusqu'à 2 SMIC). Indexation des retraites sur l'inflation. Réduction de la contribution française au budget de l'UE."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.09, "threshold": 13000},
           {"rate": 0.24, "threshold": 34000}, {"rate": 0.35, "threshold": 96000},
           {"rate": 0.41, "threshold": 210000}
         ]}
         "tva.taux_normal": 0.20
         "prestations_sociales.rsa.socle": 1.0
         "cotisations.scale": 0.85

    5. id="ps-2025"
       name="PS/Gauche sociale-démocrate — Réformes structurelles"
       description: "Proposition social-démocrate : réforme de la CSG (taux progressif : 0% jusqu'à 1,2 SMIC, 6,6% jusqu'à 2 SMIC, 9,2% au-delà), création de 4 nouvelles tranches d'IR au-delà de 200K€ (45→50→55→60%), Conditionnalité des aides aux entreprises (CIR, CII) à des engagements emploi/écologiques. Investissement massif dans la transition écologique (50Md€/an sur 5 ans) financé par un ISF climatique et un impôt européen sur les GAFA."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.11, "threshold": 11500},
           {"rate": 0.30, "threshold": 30000}, {"rate": 0.41, "threshold": 85000},
           {"rate": 0.45, "threshold": 180000}, {"rate": 0.50, "threshold": 250000},
           {"rate": 0.55, "threshold": 400000}, {"rate": 0.60, "threshold": 800000}
         ]}
         "prestations_sociales.rsa.socle": 1.15
         "prestations_sociales.prime_activite.montant_base": 1.15
         "prestations_sociales.apl.revalorisation": 1.0

    ── Pedagogical Scenarios (3) ──

    6. id="transition-ecologique-2025"
       name="Financement de la transition écologique"
       description: "Scénario pédagogique : introduction d'une taxe carbone progressive (110€/tCO2 en 2025, +10€/an), augmentation de la TVA sur les produits carbonés (taux normal porté à 25%), et investissements verts massifs (rénovation thermique, transports publics). Les recettes nouvelles sont intégralement redistribuées aux ménages modestes via une augmentation du RSA socle (+40%) et de la prime d'activité (+30%), avec un bonus APL pour les logements rénovés (classe A/B). IS inchangé dans le calcul micro — l'impact macro sur l'emploi est modélisé via la matrice des chocs. Ce scénario illustre une transition fiscalement neutre pour les bas revenus."
       parameter_overrides:
         "tva.taux_normal": 0.25
         "prestations_sociales.rsa.socle": 1.40
         "prestations_sociales.prime_activite.montant_base": 1.30
         "prestations_sociales.apl.revalorisation": 1.0
         "cotisations.scale": 1.0

    7. id="revenu-universel-2025"
       name="Revenu universel — 800€ par mois"
       description: "Scénario pédagogique : instauration d'un revenu universel mensuel de 800€ (9 600€/an) pour tout citoyen majeur, en remplacement du RSA et de la prime d'activité. Maintien des autres prestations (APL, allocations familiales) et du barème progressif de l'IR (qui s'applique désormais sur le revenu total y compris le RU, mais avec un abattement forfaitaire équivalent). Financement par une augmentation de la CSG de 9,7% à 15% et une suppression de niches fiscales. Scénario illustrant les effets d'un filet de sécurité universel."
       parameter_overrides:
         "revenu_universel.montant_mensuel": 800.00
         "revenu_universel.remplace_aides": True
         "cotisations.scale": 1.55

    8. id="equilibre-budgetaire-2025"
       name="Retour à l'équilibre budgétaire — 3% de déficit"
       description: "Scénario pédagogique : retour à un déficit public de 3% du PIB en combinant hausses d'impôts généralisées (IR +20% sur toutes les tranches, TVA à 25%, CSG à 11%) et baisse des dépenses (RSA socle -15%, allocations familiales gelées pour tous les ménages au-dessus de 40K€, APL gelées). Scénario illustrant l'effort nécessaire pour un retour rapide aux critères de Maastricht — à but pédagogique, non rattaché à un programme politique spécifique."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.132, "threshold": 11497},
           {"rate": 0.36, "threshold": 29315}, {"rate": 0.492, "threshold": 83823},
           {"rate": 0.54, "threshold": 180648}
         ]}
         "tva.taux_normal": 0.25
         "prestations_sociales.rsa.socle": 0.85
         "prestations_sociales.allocations_familiales.modulation": 0.0
         "prestations_sociales.apl.revalorisation": 0.0
         "cotisations.scale": 1.13

    ── Single-Axis Variants (3) ──

    9. id="ir-seul-2025"
       name="Variante IR seul — Baisse de 15%"
       description: "Variante isolée: seule l'impôt sur le revenu est modifié (baisse de 15% sur toutes les tranches hors tranche à 0%). Tous les autres paramètres (TVA, cotisations, prestations sociales) restent au niveau du droit en vigueur 2025. Cette variante permet de remplir l'espace d'interpolation pour la dimension IR."
       parameter_overrides:
         "impot_revenu.bareme": {"brackets": [
           {"rate": 0.00, "threshold": 0}, {"rate": 0.0935, "threshold": 11497},
           {"rate": 0.255, "threshold": 29315}, {"rate": 0.3485, "threshold": 83823},
           {"rate": 0.3825, "threshold": 180648}
         ]}

    10. id="tva-seule-2025"
        name="Variante TVA seule — Hausse de 3 points"
        description: "Variante isolée: seul le taux normal de TVA est modifié (passage de 20% à 23%). Tous les autres paramètres restent au droit en vigueur 2025. Cette variante remplit l'espace d'interpolation pour la dimension TVA."
        parameter_overrides:
          "tva.taux_normal": 0.23

    11. id="depenses-seules-2025"
        name="Variante dépenses seules — Hausse de 10%"
        description: "Variante isolée: seules les prestations sociales sont modifiées (hausse de 10% du RSA socle + prime d'activité). Tous les autres paramètres restent au droit en vigueur 2025. Cette variante remplit l'espace d'interpolation pour la dimension dépenses publiques. Note: la variation de dépenses publiques est partiellement capturée via les aides sociales dans le calcul micro — l'impact complet sur les dépenses de l'État est modélisé via le curseur macro."
        parameter_overrides:
          "prestations_sociales.rsa.socle": 1.10
          "prestations_sociales.prime_activite.montant_base": 1.10

    Update the module docstring (line 3: "Defines at least 3 scenarios" → "Defines 12 scenarios").
    Update get_scenario_definitions() docstring (line 103: "Returns at least 3 scenarios" → "Returns 12 scenarios").
    The baseline-2025 scenario MUST have empty parameter_overrides ({}).
    All ids MUST be kebab-case with -2025 suffix.
  </action>
  <acceptance_criteria>
    - get_scenario_definitions() returns exactly 12 ScenarioDefinition instances
    - python3 -c "from scenarios.scenario_definitions import get_scenario_definitions; s=get_scenario_definitions(); print(len(s)); assert len(s)>=12" exits 0
    - All scenario ids end with "-2025"
    - Baseline scenario (id="baseline-2025") has empty parameter_overrides={}
    - 5 political programs have id matching: lfi-nfp-2025, renaissance-2025, lr-2025, rn-2025, ps-2025
    - 3 pedagogical scenarios have id matching: transition-ecologique-2025, revenu-universel-2025, equilibre-budgetaire-2025
    - 3 single-axis variants have id matching: ir-seul-2025, tva-seule-2025, depenses-seules-2025
    - All scenarios have non-empty name and description (min 20 chars each)
    - All scenario ids are unique (12 distinct ids)
    - Revenu universel scenario uses keys: revenu_universel.montant_mensuel and revenu_universel.remplace_aides
    - Several scenarios use key: cotisations.scale (renaissance-2025, lr-2025, rn-2025, transition-ecologique-2025, revenu-universel-2025, equilibre-budgetaire-2025)
  </acceptance_criteria>
  <verify>
    <automated>cd packages/data-pipeline && PYTHONPATH=src python3 -c "
from scenarios.scenario_definitions import get_scenario_definitions
scenarios = get_scenario_definitions()
assert len(scenarios) >= 12, f'Expected 12+, got {len(scenarios)}'
ids = [s.id for s in scenarios]
assert len(ids) == len(set(ids)), f'Duplicate ids: {ids}'
assert 'baseline-2025' in ids
for s in scenarios:
    assert len(s.name) >= 5, f'{s.id}: name too short'
    assert len(s.description) >= 20, f'{s.id}: description too short'
    assert isinstance(s.parameter_overrides, dict), f'{s.id}: overrides not dict'
print(f'OK: {len(scenarios)} scenarios, all valid')
"</automated>
  </verify>
  <done>12 ScenarioDefinition instances returned by get_scenario_definitions(), all with unique ids, valid names/descriptions, and appropriate parameter_overrides</done>
</task>

<task type="auto">
  <name>Task 3: Extend precompute.py with cotisations.scale and revenu_universel override extraction</name>
  <files>packages/data-pipeline/src/scenarios/precompute.py</files>
  <read_first>
    packages/data-pipeline/src/scenarios/precompute.py: lines 317-404 — the override extraction block and computation section where new keys are inserted
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md: lines 259-268 — Pattern 4: new override keys (cotisations.scale, revenu_universel.montant_mensuel, revenu_universel.remplace_aides)
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-PATTERNS.md: lines 329-410 — exact code to insert for extraction + computation
    packages/data-pipeline/src/scenarios/precompute.py: lines 157-172 — _estimate_cotisations() signature (salaire_total, scale) — the function already accepts scale, but it's hardcoded to 1.0 in the call site
  </read_first>
  <action>
    Modify file: packages/data-pipeline/src/scenarios/precompute.py

    Two insertions, both in _compute_scenario_result():

    ── Insertion 1: After line 353 (existing override extraction block) ──

    Add these new extraction blocks after the taux_tva extraction (after line 353), before the "# ── Compute tax components ──" comment:

    ```python
    # Cotisations scale factor
    cotisation_scale = 1.0
    if "cotisations.scale" in overrides:
        cotisation_scale = float(overrides["cotisations.scale"])

    # Revenu universel
    ru_montant = 0.0
    ru_remplace_aides = False
    if "revenu_universel.montant_mensuel" in overrides:
        ru_montant = float(overrides["revenu_universel.montant_mensuel"])
    if "revenu_universel.remplace_aides" in overrides:
        ru_remplace_aides = bool(overrides["revenu_universel.remplace_aides"])
    ```

    ── Insertion 2: Modify the computation section (lines 370-394) ──

    Change line 371: _estimate_cotisations(salaire_total) → _estimate_cotisations(salaire_total, scale=cotisation_scale)

    After line 378 (aides_totales = sum(aides_dict.values())), insert revenu universel logic before the revenu_disponible computation:

    ```python
    # Revenu universel: flat monthly transfer, optionally replacing RSA + prime activité
    if ru_montant > 0:
        ru_annuel = ru_montant * 12
        aides_totales += ru_annuel
        if ru_remplace_aides:
            # Subtract RSA and prime d'activité if they were included
            aides_totales -= (aides_dict.get("rsa", 0.0) + aides_dict.get("prime_activite", 0.0))
            aides_totales = max(aides_totales, ru_annuel)  # At minimum, the RU amount
    ```

    ── No other changes ──

    Do NOT modify:
    - The result export dict structure (lines 397-404) — stays unchanged
    - The JSON key stringification (line 473) — stays unchanged
    - _estimate_cotisations() function itself (lines 157-172) — it already accepts scale parameter
    - _estimate_aides() function itself (lines 175-250) — RU logic is handled at the caller level, not inside _estimate_aides()
    - Lazy import patterns or error handling
  </action>
  <acceptance_criteria>
    - _compute_scenario_result() extracts "cotisations.scale" → cotisation_scale variable (default 1.0)
    - _compute_scenario_result() extracts "revenu_universel.montant_mensuel" → ru_montant variable (default 0.0)
    - _compute_scenario_result() extracts "revenu_universel.remplace_aides" → ru_remplace_aides variable (default False)
    - _estimate_cotisations() called with scale=cotisation_scale (not hardcoded 1.0)
    - When ru_montant > 0 and ru_remplace_aides is True: aides_totales subtracts rsa + prime_activite before adding ru_annuel
    - When no override keys present: behavior is identical to current (backward compatible)
    - grep -n "cotisation_scale" packages/data-pipeline/src/scenarios/precompute.py finds the variable
    - grep -n "ru_montant" packages/data-pipeline/src/scenarios/precompute.py finds the variable
    - grep -n "scale=cotisation_scale" packages/data-pipeline/src/scenarios/precompute.py finds the call
  </acceptance_criteria>
  <verify>
    <automated>cd packages/data-pipeline && PYTHONPATH=src python3 -c "
from scenarios.precompute import _compute_scenario_result
from scenarios.scenario_definitions import ScenarioDefinition, get_scenario_definitions

# Test 1: Backward compatibility — no new overrides
baseline = [s for s in get_scenario_definitions() if s.id == 'baseline-2025'][0]
profile = {'input': {'situation_familiale': 'celibataire', 'nb_enfants': 0, 'revenus': {'salaires': [24000.0]}, 'zone_residence': 'zone2'}}
result = _compute_scenario_result(profile, baseline, 0)
assert result['is'] == 0.0, 'IS must be 0.0'

# Test 2: Backward compatibility — existing overrides still work
expansion = [s for s in get_scenario_definitions() if s.id == 'expansion-2025'][0]
result = _compute_scenario_result(profile, expansion, 0)
assert result['is'] == 0.0, 'IS must be 0.0 with expansion scenario'

print('OK: backward compatibility verified')
"</automated>
  </verify>
  <done>precompute.py _compute_scenario_result() extracts cotisations.scale and revenu_universel overrides, _estimate_cotisations() uses cotisation_scale parameter, revenu universel logic adds flat transfer optionally replacing RSA+prime activité, backward compatible with existing 3 scenarios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario_definitions.py → precompute.py | Scenario parameter_overrides dict passes from definition (trusted Python source) to extraction block — no external input crosses this boundary |
| precompute.py → scenarios-v2025.1.json | Pre-computed results serialized to JSON file — must match the TypeScript consumer contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | scenarios-v2025.1.json output | mitigate | JSON Schema (Draft 2020-12) validates output structure, field types, value ranges, and required fields via `jsonschema` before TypeScript tests consume the output (per D-08) |
| T-02.1-02 | Repudiation | openfisca-france version tracking | mitigate | openfisca-france version recorded in output metadata (`openfisca_france_version`); CI version gate warns on out-of-range versions; exact version traceable in every generated artifact |
| T-02.1-03 | Tampering | Bilingual test fixtures | accept | Fixtures committed to git with review; pre-compute results cross-validated against fixture expected values; source of truth is the openfisca-france Python reference — no external untrusted input |
| T-02.1-04 | Tampering | Scenario parameter overrides injection | accept | parameter_overrides are hardcoded in Python source (`scenario_definitions.py`) — no user input reaches the pipeline at runtime; modifications require code review on PR |
| T-02.1-05 | Tampering | Pipeline output between CI jobs | mitigate | `typescript-tests` job depends on `scenario-precompute` in the same workflow; artifact sharing via checkout; output file committed to repo as static asset with git integrity |
</threat_model>

<verification>
- [ ] scenarios.schema.json validates as Draft 2020-12 via jsonschema Draft202012Validator.check_schema()
- [ ] get_scenario_definitions() returns 12 ScenarioDefinition instances
- [ ] All 12 scenario ids are unique and use kebab-case with -2025 suffix
- [ ] Baseline scenario has empty parameter_overrides
- [ ] precompute.py _estimate_cotisations() uses scale=cotisation_scale parameter
- [ ] Revenu universel extraction logic added but backward-compatible (no-op when not present)
- [ ] Existing 3 scenarios still compute correctly (backward compatibility)
</verification>

<success_criteria>
- 12 ScenarioDefinition instances enumerable via get_scenario_definitions()
- 5 political programs with IR bareme + social benefit overrides matching publicly stated platforms
- 3 pedagogical scenarios with extended override keys (cotisations.scale, revenu_universel)
- 3 single-axis variants with symmetric ± values for IR, TVA, spending dimensions
- JSON Schema validates as Draft 2020-12 with minItems: 12, const: 0.0 for IS, const: "v2025.1" for version
- _compute_scenario_result() backward compatible: existing 3 scenarios produce identical results to before
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-01-SUMMARY.md`
</output>
