---
phase: 02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu
plan: 03
type: execute
wave: 2
depends_on:
  - 02.1-01
  - 02.1-02
files_modified:
  - .github/workflows/phase2-wasm.yml
  - packages/data-pipeline/dist/scenarios-v2025.1.json
autonomous: false
requirements:
  - MICRO-01
  - MICRO-02
  - MICRO-03
  - MICRO-05

must_haves:
  truths:
    - "CI scenario-precompute job passes with >= 12 scenarios produced"
    - "CI JSON Schema validation step passes on the generated scenarios-v2025.1.json"
    - "CI typescript-tests job passes including the integration test"
    - "scenarios-v2025.1.json is generated locally and committed to the repository"
    - "Pipeline runs successfully in local dev environment using Python 3.10-3.13 + openfisca-france"
  artifacts:
    - path: ".github/workflows/phase2-wasm.yml"
      provides: "Updated CI workflow with count gate 3→12, added schema validation step"
      contains: "-lt 12"
    - path: "packages/data-pipeline/dist/scenarios-v2025.1.json"
      provides: "Pre-computed scenario results: 12 scenarios × 32 profiles"
      contains: "\"total_scenarios\": 12"
  key_links:
    - from: "CI scenario-precompute job"
      to: "CI typescript-tests job"
      via: "needs: [scenario-precompute]"
      pattern: "needs.*scenario-precompute"
    - from: "scenarios-v2025.1.json"
      to: "ScenarioCache.loadFromJSON()"
      via: "main thread fetch()"
      pattern: "fetch.*scenarios-v2025.1"
---

<objective>
Update the CI workflow for the expanded pipeline (count gate 3→12, add JSON Schema validation step), execute the pre-compute pipeline locally to generate scenarios-v2025.1.json, commit the output as a static asset, and verify all tests pass.

Purpose: Without CI gates enforcing the 12-scenario minimum and schema contract, a silently-truncated pipeline output would reach Phase 3 with incomplete data. Without a committed scenarios-v2025.1.json, the TypeScript integration test cannot run and Phase 3 cannot consume real data.
Output: Updated CI workflow, generated and committed scenarios-v2025.1.json, all tests passing.
</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-CONTEXT.md
@.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md
@.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-PATTERNS.md

<interfaces>
<!-- Key CI patterns and pipeline execution context. -->

From .github/workflows/phase2-wasm.yml (CI job structure):
```yaml
scenario-precompute:
  needs: [phase1-gate]
  timeout-minutes: 30
  steps:
    - uses: actions/setup-python@v5
      with: { python-version: '3.10' }
    - run: pip install ".[dev]"
    - run: PYTHONPATH=src python -m scenarios.precompute
    - name: Verify scenario output
      run: |
        SCENARIO_COUNT=$(python3 -c "import json; d=json.load(open('$OUTPUT')); print(len(d['scenarios']))")
        if [ "$SCENARIO_COUNT" -lt 3 ]; then exit 1; fi
```

From packages/data-pipeline/src/scenarios/precompute.py (pipeline entry):
```python
def precompute_scenarios(scenarios=None, profiles=None, output_dir=None, reference_year=2025) -> str:
def main() -> None:
    output_path = precompute_scenarios()
```

From packages/data-pipeline/pyproject.toml (pipeline script):
```toml
[project.scripts]
precompute-scenarios = "scenarios.precompute:main"

dependencies = [
    "openfisca-france>=159,<200",
    "jsonschema==4.25.1",
    ...
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI workflow — count gate 3→12, add schema validation step</name>
  <files>.github/workflows/phase2-wasm.yml</files>
  <read_first>
    .github/workflows/phase2-wasm.yml: lines 184-205 — the "Verify scenario output" step to modify and the location after it where schema validation step is inserted
    .github/workflows/phase2-wasm.yml: lines 218-240 — the typescript-tests job dependency (needs: [scenario-precompute])
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-PATTERNS.md: lines 445-512 — exact CI modifications with line numbers and code to insert
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md: lines 296-301 — Pitfall 3: CI scenario count gate failure
  </read_first>
  <action>
    Modify file: .github/workflows/phase2-wasm.yml

    Two changes, both in the scenario-precompute job:

    ── Change 1: Update count gate from 3 to 12 ──

    Line 194: Change "-lt 3" to "-lt 12"
    Line 195: Change error message "need >= 3" to "need >= 12"

    The changed block should read:
    ```yaml
    if [ "$SCENARIO_COUNT" -lt 12 ]; then
      echo "::error file=$OUTPUT,line=1::Scenario pre-compute FAILED: only $SCENARIO_COUNT scenarios (need >= 12)"
      exit 1
    fi
    echo "PASSED: $SCENARIO_COUNT scenarios pre-computed"
    ```

    ── Change 2: Add JSON Schema validation step ──

    After the "Verify scenario output" step (after line 205, the file size check), before "Verify openfisca-france version consistency" (line 207), insert a new step:

    ```yaml
          - name: Validate scenarios JSON Schema
            run: |
              SCHEMA="packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json"
              OUTPUT="packages/data-pipeline/dist/scenarios-v2025.1.json"
              python3 -c "
    import json, sys
    from jsonschema import Draft202012Validator, ValidationError

    with open('$SCHEMA') as f:
        schema = json.load(f)
    with open('$OUTPUT') as f:
        doc = json.load(f)

    validator = Draft202012Validator(schema)
    errors = list(validator.iter_errors(doc))

    if errors:
        for e in errors:
            print(f'SCHEMA ERROR: {e.message} at path {\" → \".join(str(p) for p in e.absolute_path) if e.absolute_path else \"root\"}', file=sys.stderr)
        sys.exit(1)
    print(f'PASSED: scenarios JSON validates against schema ({len(doc[\"scenarios\"])} scenarios, {len(doc[\"scenarios\"][0][\"results\"])} profiles)')
    "
    ```

    ── No other changes ──

    Do NOT modify:
    - The job dependency chain (needs: [phase1-gate])
    - The typescript-tests job (already depends on scenario-precompute — unchanged)
    - The CI summary gate (ci-summary job already lists scenario-precompute)
    - Version check gate (still warns on out-of-range openfisca-france versions)
    - Any other jobs or steps
  </action>
  <acceptance_criteria>
    - .github/workflows/phase2-wasm.yml line 194 reads "-lt 12" (not "-lt 3")
    - .github/workflows/phase2-wasm.yml contains step named "Validate scenarios JSON Schema"
    - Schema validation step uses Draft202012Validator from jsonschema
    - Schema validation step is placed between "Verify scenario output" and "Verify openfisca-france version consistency" steps
    - grep "-lt 12" .github/workflows/phase2-wasm.yml returns exactly 1 match in the scenario-precompute job
    - grep "Validate scenarios JSON Schema" .github/workflows/phase2-wasm.yml returns exactly 1 match
    - grep "Draft202012Validator" .github/workflows/phase2-wasm.yml returns at least 1 match
    - The dependencies field in typescript-tests (needs: [scenario-precompute]) is unchanged
  </acceptance_criteria>
  <verify>
    <automated>grep -n "lt 12" .github/workflows/phase2-wasm.yml && grep -n "Validate scenarios JSON Schema" .github/workflows/phase2-wasm.yml</automated>
  </verify>
  <done>CI workflow updated: count gate 3→12, JSON Schema validation step added between scenario verification and version consistency check</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Run the pre-compute pipeline locally and verify output</name>
  <files>packages/data-pipeline/dist/scenarios-v2025.1.json</files>
  <action>Present the pre-compute pipeline execution instructions and verification steps to the user. The agent is blocked here — openfisca-france requires Python ≤ 3.13 but the local environment runs Python 3.14.5. The user must create a compatible virtualenv with `uv venv --python 3.10`, install dependencies, run the pipeline, verify the output, and signal completion.</action>
  <read_first>
    packages/data-pipeline/src/scenarios/precompute.py: lines 410-540 — precompute_scenarios() function, CLI entry point main(), output path resolution
    packages/data-pipeline/src/scenarios/scenario_definitions.py — the 12 scenarios to compute
    packages/data-pipeline/pyproject.toml: lines 31-33 — precompute-scenarios script entry point
    .planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-RESEARCH.md: lines 82-95 — Python version requirement (3.10-3.13), uv venv setup
  </read_first>
  <what-built>The Python scenario pre-compute pipeline (scenario_definitions.py + precompute.py) is ready with 12 scenarios and extended override extraction. The CI workflow is updated. The scenarios-v2025.1.json output file needs to be generated locally and committed.</what-built>
  <how-to-verify>
    **CRITICAL: openfisca-france 175.0.40 requires Python ≤ 3.13. Your local Python (3.14.5) is incompatible.**

    Step 1: Create a Python 3.10 virtualenv and install dependencies:
    ```bash
    cd packages/data-pipeline
    uv venv --python 3.10
    source .venv/bin/activate
    uv pip install ".[dev]"
    ```

    Step 2: Verify openfisca-france is importable:
    ```bash
    python3 -c "import openfisca_france; print(openfisca_france.__version__)"
    ```

    Step 3: Run the pre-compute pipeline:
    ```bash
    PYTHONPATH=src python -m scenarios.precompute
    ```

    Step 4: Verify the output:
    ```bash
    # Check file exists
    ls -lh packages/data-pipeline/dist/scenarios-v2025.1.json
    
    # Check scenario count
    python3 -c "import json; d=json.load(open('packages/data-pipeline/dist/scenarios-v2025.1.json')); print(f'{len(d[\"scenarios\"])} scenarios, {len(d[\"scenarios\"][0][\"results\"])} profiles per scenario')"
    # Expected: 12 scenarios, 32 profiles per scenario
    
    # Check baseline results
    python3 -c "
    import json
    d=json.load(open('packages/data-pipeline/dist/scenarios-v2025.1.json'))
    baseline = [s for s in d['scenarios'] if s['definition']['id'] == 'baseline-2025'][0]
    r = baseline['results']['0']
    print(f'IR: {r[\"ir\"]}, IS: {r[\"is\"]}, TVA: {r[\"tva\"]}, Cotisations: {r[\"cotisations\"]}, Aides: {r[\"aides\"]}, Revenu dispo: {r[\"revenuDisponible\"]}')
    "
    ```

    Step 5: Run all tests:
    ```bash
    # Python tests
    cd packages/data-pipeline
    python3 -m pytest tests/ -v
    
    # TypeScript tests (including integration)
    cd webapp
    npx vitest run --reporter=verbose
    ```

    Step 6: Commit the output:
    ```bash
    git add packages/data-pipeline/dist/scenarios-v2025.1.json
    git commit -m "feat(02.1): generate 12-scenario pre-compute output (scenarios-v2025.1.json)"
    ```
  </how-to-verify>
  <resume-signal>Type "approved" once the pipeline runs successfully and all tests pass, or describe any issues encountered.</resume-signal>
  <acceptance_criteria>
    - scénario pre-compute pipeline runs without errors
    - packages/data-pipeline/dist/scenarios-v2025.1.json exists
    - JSON contains exactly 12 scenarios in "scenarios" array
    - Each scenario has exactly 32 profile results (string keys "0" through "31")
    - IS value is 0.0 for all results (spot-check baseline + 2 others)
    - All revenuDisponible values are > 0
    - All ir, tva, cotisations, aides values are >= 0
    - Metadata.version == "v2025.1"
    - Metadata.total_scenarios == 12
    - Metadata.total_profiles == 32
    - JSON Schema validation passes: python3 -c "... Draft202012Validator(schema).iter_errors(doc) ..." exits 0 with no errors
    - Python tests pass: pytest exits 0
    - TypeScript tests pass: npx vitest run exits 0 (integration test loads real JSON)
  </acceptance_criteria>
  <verify>
    <automated>
python3 -c "
import json
with open('packages/data-pipeline/dist/scenarios-v2025.1.json') as f:
    d = json.load(f)
assert len(d['scenarios']) >= 12, f'Expected 12+, got {len(d[\"scenarios\"])}'
for s in d['scenarios']:
    assert len(s['results']) == 32, f'{s[\"definition\"][\"id\"]}: expected 32 results, got {len(s[\"results\"])}'
    for k, v in s['results'].items():
        assert v['is'] == 0.0, f'{s[\"definition\"][\"id\"]}[{k}]: IS must be 0.0'
        assert v['revenuDisponible'] > 0, f'{s[\"definition\"][\"id\"]}[{k}]: revenuDisponible must be > 0'
assert d['metadata']['total_scenarios'] == 12
assert d['metadata']['total_profiles'] == 32
assert d['metadata']['version'] == 'v2025.1'
print(f'OK: {d[\"metadata\"][\"total_scenarios\"]} scenarios, {d[\"metadata\"][\"total_profiles\"]} profiles per scenario')
"</automated>
  </verify>
  <done>scenarios-v2025.1.json generated (12 scenarios × 32 profiles), all validations pass, committed to repository</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local openfisca-france → precompute.py | Python computation runs trusted formulas against trusted fixture data; no external input crosses this boundary |
| precompute.py → scenarios-v2025.1.json | Output file committed to git — integrity protected by git hashes and code review |
| scenarios-v2025.1.json → TypeScript ScenarioCache | JSON consumed by main-thread fetch() or postMessage; parse errors are caught by try/catch in loadFromJSON() |
| CI scenario-precompute job → CI typescript-tests job | Same workflow, artifact sharing via checkout; output file is committed — CI job order enforces freshness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | scenarios-v2025.1.json output | mitigate | JSON Schema validation in CI step (Task 1 — Draft 2020-12) gates output before TypeScript tests consume it |
| T-02.1-02 | Repudiation | openfisca-france version | mitigate | Version recorded in metadata ("openfisca_france_version" field); CI version gate warns on out-of-range versions (lines 208-216) |
| T-02.1-05 | Tampering | Pipeline output between CI jobs | mitigate | typescript-tests depends on scenario-precompute (same workflow); output committed as static asset with git integrity |
| T-02.1-08 | Tampering | Schema validation bypass | mitigate | Schema validation step runs in the same CI job that produces output — cannot be skipped independently; both count gate AND schema must pass |
</threat_model>

<verification>
- [ ] CI workflow count gate changed from 3 to 12
- [ ] CI workflow includes JSON Schema validation step
- [ ] scenarios-v2025.1.json contains 12 scenarios × 32 profiles
- [ ] IS always 0.0, revenuDisponible always > 0
- [ ] Schema validation passes on the generated output
- [ ] TypeScript integration test passes with real data
- [ ] All Python tests pass
- [ ] Output file committed to git
</verification>

<success_criteria>
- CI workflow enforces >= 12 scenarios and JSON Schema validation
- scenarios-v2025.1.json generated locally, passes all validations, committed to repo
- All Python and TypeScript tests pass (including integration test with real data)
- Phase 3 can consume scenarios-v2025.1.json via ScenarioCache.loadFromJSON() for citizen-mode lookups
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu/02.1-03-SUMMARY.md`
</output>
