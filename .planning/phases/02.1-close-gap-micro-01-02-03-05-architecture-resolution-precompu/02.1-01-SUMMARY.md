---
phase: 02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu
plan: 01
subsystem: data-pipeline
tags: [scenario-definitions, json-schema, precompute, openfisca-france, cotisations, revenu-universel]

requires:
  - phase: 02-core-simulation-engines-wasm
    provides: scenario data format, pre-compute pipeline infrastructure, ScenarioDefinition dataclass
provides:
  - JSON Schema (Draft 2020-12) for scenarios-v2025.1.json output validation
  - 14 ScenarioDefinition instances (3 existing + 11 new) in get_scenario_definitions()
  - Extended override extraction for cotisations.scale and revenu_universel keys in precompute.py
affects: [02.1-02, 02.1-03, 03-interactive-simulation-shell-mvp]

tech-stack:
  added: []
  patterns:
    - "ScenarioDefinition dataclass with lazy openfisca imports and Reform subclass pattern"
    - "Override extraction with default values (1.0 for scale, 0.0 for RU, False for replaces)"
    - "Backward-compatible extraction: existing scenarios unchanged when new keys are absent"

key-files:
  created:
    - packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json
  modified:
    - packages/data-pipeline/src/scenarios/scenario_definitions.py
    - packages/data-pipeline/src/scenarios/precompute.py

key-decisions:
  - "Plan content discrepancy: listed 11 new scenarios (5 political + 3 pedagogical + 3 single-axis) but stated 9 new. Followed task content — 14 total scenarios."
  - "JSON Schema validated as Draft 2020-12 via Draft202012Validator in project venv"
  - "The revenu universel override extraction uses ru_montant > 0 gate with max(aides_totales, ru_annuel) floor"

patterns-established:
  - "Pattern: Default-value extraction — all new override keys default to no-op values (cotisation_scale=1.0, ru_montant=0.0, ru_remplace_aides=False) ensuring backward compatibility"
  - "Pattern: Scenario grouping comments — '# ── Political Program ──', '# ── Pedagogical Scenario ──', '# ── Single-Axis Variant ──' block headers"

requirements-completed: [MICRO-01, MICRO-02, MICRO-03]

# Metrics
duration: 9 min
completed: 2026-05-15
---

# Phase 02.1 Plan 01: Scenario Catalog Expansion & Pipeline Extension Summary

**14 scenario definitions (3 existing + 11 new), JSON Schema contract for output validation, and extended precompute override extraction for cotisations scale and revenu universel keys.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-05-15T05:56:26Z
- **Completed:** 2026-05-15T06:05:47Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- JSON Schema (Draft 2020-12) for scenarios-v2025.1.json with minItems: 12, IS const 0.0, version const v2025.1, total_profiles const 32
- 14 ScenarioDefinition instances: 3 existing + 5 political programs (LFI/NFP, Renaissance, LR, RN, PS) + 3 pedagogical (transition-ecologique, revenu-universel, equilibre-budgetaire) + 3 single-axis variants (ir-seul, tva-seule, depenses-seules)
- Extended precompute.py with cotisations.scale extraction (replaces hardcoded scale=1.0 in _estimate_cotisations) and revenu_universel.montant_mensuel + revenu_universel.remplace_aides extraction with flat transfer + optional RSA/prime-activite replacement logic
- Backward compatibility: existing 3 scenarios produce identical results after new extraction blocks

## Task Commits

1. **Task 1: Create JSON Schema for scenarios-v2025.1.json** - `724839b` (feat(02.1-01): create JSON Schema for scenarios-v2025.1.json output document)
2. **Task 2: Expand scenario definitions from 3 to 14** - `6fd1ce1` (feat(02.1-01): expand scenario definitions from 3 to 14 scenarios with political, pedagogical, and single-axis variants)
3. **Task 3: Extend precompute.py with new override extraction** - `e58554a` (feat(02.1-01): add cotisations.scale and revenu universel override extraction to precompute pipeline)

## Files Created/Modified

- `packages/data-pipeline/src/scenarios/schemas/scenarios.schema.json` — JSON Schema (Draft 2020-12) enforcing 12+ scenarios, IS=0.0, metadata contract
- `packages/data-pipeline/src/scenarios/scenario_definitions.py` — 14 ScenarioDefinition instances with bracket tables, social benefit scales, and new override keys
- `packages/data-pipeline/src/scenarios/precompute.py` — cotisation_scale extraction, revenu universel flat transfer with optional RSA/prime-activite replacement

## Decisions Made

- **Plan content discrepancy:** Task text said "add 9 new" but listed 11 (5 political + 3 pedagogical + 3 single-axis). Followed actual task content — 14 total scenarios documented in docstrings.
- **JSON Schema validation:** Required creating project venv with uv + jsonschema (system Python 3.14.5 is uv-managed, no system packages allowed per PEP 668).
- **Backward compatibility:** All new override keys default to no-op values (cotisation_scale=1.0, ru_montant=0.0, ru_remplace_aides=False) — existing 3 scenarios compute identically.

## Deviations from Plan

None — plan executed exactly as written, except the content discrepancy noted above (docstrings say 14 to match actual count).

## Issues Encountered

- Python 3.14.5 uv-managed environment required `uv venv` + `uv pip install jsonschema` for Draft202012Validator verification (PEP 668 blocked system install)

## Next Phase Readiness

- Scenario catalog and JSON Schema ready for test scaffolding (Plan 02.1-02)
- Precompute pipeline extraction ready for CI execution, test validation, and scenarios-v2025.1.json generation (Plan 02.1-03)

---
*Phase: 02.1-close-gap-micro-01-02-03-05-architecture-resolution-precompu*
*Completed: 2026-05-15*
