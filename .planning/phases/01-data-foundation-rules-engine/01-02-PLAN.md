---
phase: 01-data-foundation-rules-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/data-pipeline/src/synthetic_pop/__init__.py
  - packages/data-pipeline/src/synthetic_pop/preprocess.py
  - packages/data-pipeline/src/synthetic_pop/train.py
  - packages/data-pipeline/src/synthetic_pop/dp_inject.py
  - packages/data-pipeline/src/synthetic_pop/evaluate.py
  - packages/data-pipeline/src/synthetic_pop/export.py
autonomous: true
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "CopulaGAN can train on CASD/INSEE real microdata to produce 50,000 synthetic profiles with preserved age-patrimony-income correlations"
    - "OpenDP formally proves epsilon <= 1.0 via .map(d_in=1) — not estimated, not hand-rolled Laplacian noise"
    - "SDMetrics QualityReport validates statistical fidelity and DisclosureProtectionEstimate validates privacy protection"
    - "Synthetic dataset exports as JSON with SHA-256 integrity hash and version tag population-v2025.1"
  artifacts:
    - path: "packages/data-pipeline/src/synthetic_pop/preprocess.py"
      provides: "Real data loading, outlier handling, categorical encoding, SDV SingleTableMetadata"
      min_lines: 60
    - path: "packages/data-pipeline/src/synthetic_pop/train.py"
      provides: "CopulaGANSynthesizer training with enforce_min_max=True and epoch=500"
      contains: "CopulaGANSynthesizer"
    - path: "packages/data-pipeline/src/synthetic_pop/dp_inject.py"
      provides: "OpenDP Laplace mechanism with formal epsilon proof via .map(d_in=1)"
      contains: "opendp.prelude"
  key_links:
    - from: "preprocess.py"
      to: "train.py"
      via: "DataFrame + SingleTableMetadata"
      pattern: "synthesizer.fit"
    - from: "dp_inject.py"
      to: "OpenDP 0.14.2"
      via: "opendp.prelude, .map(d_in=1)"
      pattern: "make_laplace"
---

<objective>
Build the synthetic population generation pipeline: CopulaGAN training on real microdata, OpenDP differential privacy with formal epsilon <= 1.0 proof, SDMetrics quality + privacy evaluation, and JSON export with integrity hashes.

Purpose: DATA-02 + DATA-03 — produce 50,000 synthetic profiles with CNIL-auditable DP guarantees.
Output: packages/data-pipeline/src/synthetic_pop/ with 5 modules: preprocess, train, dp_inject, evaluate, export.
</objective>

<execution_context>
@/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation-rules-engine/01-CONTEXT.md
@.planning/phases/01-data-foundation-rules-engine/01-RESEARCH.md

<interfaces>
From packages/data-pipeline/pyproject.toml (Plan 01): sdv==1.36.1, opendp==0.14.2, sdmetrics, numpy, scipy, pandas.

Output for downstream: population-v2025.1.json (50K profiles), population-v2025.1.meta.json (hash + epsilon value + metadata).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data preprocessing module (SDV metadata) and CopulaGAN training script</name>
  <files>
    packages/data-pipeline/src/synthetic_pop/__init__.py
    packages/data-pipeline/src/synthetic_pop/preprocess.py
    packages/data-pipeline/src/synthetic_pop/train.py
  </files>
  <read_first>
    .planning/phases/01-data-foundation-rules-engine/01-CONTEXT.md (D-04, D-06)
    .planning/phases/01-data-foundation-rules-engine/01-RESEARCH.md (Pattern 3, lines 378-418)
    .planning/research/STACK.md (SDV 1.36.1 versions)
    packages/data-pipeline/pyproject.toml (exact pin versions)
  </read_first>
  <action>
    **per D-04, D-06**

    1. Create __init__.py exporting: preprocess_real_data, build_metadata, train_synthesizer, generate_synthetic_population.

    2. Create preprocess.py with these functions:
       - load_real_data(source_path, source_type="csv") -> pd.DataFrame: Loads CASD or INSEE ERFS CSV/Parquet. If file nonexistent, returns a 10-row placeholder DataFrame with columns [profile_id, age, patrimoine, revenu_fiscal, situation_familiale, nombre_parts, type_activite, zone_residence] and reasonable defaults. Logs warning for fallback.
       - preprocess_real_data(df) -> pd.DataFrame: Clip age [18, 100], patrimoine [0, p99], revenu_fiscal [0, p99.9]. Drop null revenu_fiscal or age. Add profile_id if missing. Log preprocessing stats.
       - build_metadata(df) -> SingleTableMetadata: Create SDV SingleTableMetadata with per-column sdtype: age (numerical, 0-120), patrimoine (numerical), revenu_fiscal (numerical), situation_familiale (categorical: celibataire, marie, pacse, veuf, divorce), nombre_parts (numerical, 1.0-10.0), type_activite (categorical: salarie, independant, retraite, chomeur, etudiant, inactif), zone_residence (categorical: zone1, zone2, zone3). Set primary_key="profile_id". Detect remaining via metadata.detect_from_dataframe(df).
       - Per D-06: Accept TOKENIZED_PATH env var for CASD path; warn when falling back.

    3. Create train.py with:
       - train_synthesizer(metadata, real_data, epochs=500) -> CopulaGANSynthesizer: Instantiate CopulaGANSynthesizer with enforce_min_max_values=True, enforce_rounding=True, numerical_distributions={"age": "beta", "patrimoine": "gamma", "revenu_fiscal": "gamma"}, epochs=epochs, verbose=True. Call synthesizer.fit(real_data). Save model via synthesizer.save() to checkpoint.
       - generate_synthetic_population(synthesizer, num_rows=50000) -> pd.DataFrame: Call synthesizer.sample(num_rows). Assert no nulls in critical columns. Return DataFrame.
  </action>
  <acceptance_criteria>
    - preprocess.py imports from sdv.metadata import SingleTableMetadata
    - train.py imports from sdv.single_table import CopulaGANSynthesizer
    - train.py contains enforce_min_max_values=True and epochs=500 as default parameter
    - grep -c "SingleTableMetadata" packages/data-pipeline/src/synthetic_pop/preprocess.py returns >= 1
    - grep -c "CopulaGANSynthesizer" packages/data-pipeline/src/synthetic_pop/train.py returns >= 1
    - python3 -c "from synthetic_pop.preprocess import preprocess_real_data, build_metadata" succeeds from packages/data-pipeline/src
    - python3 -c "from synthetic_pop.train import train_synthesizer, generate_synthetic_population" succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd packages/data-pipeline/src && python3 -c "
from synthetic_pop.preprocess import preprocess_real_data, build_metadata, load_real_data
from synthetic_pop.train import train_synthesizer, generate_synthetic_population
import inspect
sig = inspect.signature(train_synthesizer)
assert sig.parameters['epochs'].default == 500, 'epochs default must be 500'
print('OK: all imports and signatures verified')
"</automated>
  </verify>
  <done>All 3 files import without errors. CopulaGAN configured per RESEARCH.md. Pipeline runs end-to-end on fallback data.</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenDP DP injection, SDMetrics evaluation, and JSON export modules</name>
  <files>
    packages/data-pipeline/src/synthetic_pop/dp_inject.py
    packages/data-pipeline/src/synthetic_pop/evaluate.py
    packages/data-pipeline/src/synthetic_pop/export.py
  </files>
  <read_first>
    .planning/phases/01-data-foundation-rules-engine/01-CONTEXT.md (D-05, D-07)
    .planning/phases/01-data-foundation-rules-engine/01-RESEARCH.md (Pattern 4 lines 420-461; Pattern 6 lines 650-676; anti-pattern lines 538-543; Pitfall 1 lines 560-565)
    packages/data-pipeline/src/synthetic_pop/preprocess.py (Task 1 output — understand types)
  </read_first>
  <action>
    **per D-05, D-07**

    1. Create dp_inject.py per RESEARCH.md Pattern 4:
       - compute_aggregates(data: pd.DataFrame) -> dict: Pre-compute decile boundaries, Gini coefficient, mean/median for revenu_fiscal, patrimoine, age. One-time DP budget approach per Pitfall 1.
       - prove_dp_guarantee(data: list[float], epsilon_target=1.0) -> dict: Use opendp.prelude (NOT numpy.random!). Follow Pattern 4 structure: dp.atom_domain(T=float, nan=False) -> dp.t.make_clamp() -> dp.t.make_bounded_sum() -> compose with >> -> dp.m.make_laplace() -> call .map(d_in=1) for formal epsilon proof. Returns {"actual_epsilon": float, "within_budget": bool, "noisy_sum": float, "mechanism": "laplace"}.
       - inject_dp_privacy(synthetic_df: pd.DataFrame, epsilon_budget=1.0) -> tuple: Apply DP noise to PRE-COMPUTED AGGREGATES only (not individual profiles). Mean revenu per decile at epsilon/5 each, mean patrimoine per age bracket at epsilon/3 each, Gini at epsilon/2. Track via dp.make_basic_composition(). Returns (original_df_unmodified, dp_report).
       - build_privacy_statement(dp_report, reference_year=2025) -> str: French-language CNIL-compliant privacy statement.
       - MODULE-LEVEL RULE: Never use numpy.random.laplace. OpenDP's .map(d_in=1) is the formal proof.

    2. Create evaluate.py per RESEARCH.md Pattern 6:
       - evaluate_quality(real_data, synthetic_data, metadata, output_dir) -> dict: SDMetrics QualityReport().generate(), extract overall/column_shapes/column_pair_trends scores. Save to output_dir/quality_report.json.
       - evaluate_privacy(real_data, synthetic_data) -> dict: DisclosureProtectionEstimate.compute() with known_column_names=["age","situation_familiale"], sensitive_column_names=["patrimoine","revenu_fiscal"], num_rows_subsample=5000, num_iterations=50. Returns {"dpe_score": float, "is_protected": bool} where dpe_score >= 0.7.
       - generate_quality_report(...) -> dict: Combine fidelity + privacy scores.

    3. Create export.py:
       - export_synthetic_population(synthetic_df, dp_report, output_path, reference_year=2025) -> str: Write JSON with orient="records", indent=2, ensure_ascii=False. Compute SHA-256 hash of output. Write sidecar {output_path}.meta.json with version="population-v2025.1", reference_year=2025, num_profiles, dp_epsilon from dp_report, sha256, generation_timestamp ISO8601, data_source string.
       - compute_sha256(filepath) -> str: hashlib.sha256 with 8KB chunked read.
       - Assert len(synthetic_df) == 50000 for production; warn-only for dev.
  </action>
  <acceptance_criteria>
    - dp_inject.py contains "import opendp.prelude" and ".map(d_in=" (formal proof per D-05)
    - dp_inject.py does NOT contain "numpy.random.laplace" or "np.random.laplace" (anti-pattern check)
    - evaluate.py contains "QualityReport" and "DisclosureProtectionEstimate" (per D-07)
    - export.py contains "sha256" and "population-v2025.1" (per D-16)
    - grep -c "numpy.random.laplace\|np.random.laplace" packages/data-pipeline/src/synthetic_pop/dp_inject.py returns 0
    - python3 -c "from synthetic_pop.dp_inject import prove_dp_guarantee, inject_dp_privacy; from synthetic_pop.evaluate import evaluate_quality, evaluate_privacy; from synthetic_pop.export import export_synthetic_population, compute_sha256; print('all OK')" succeeds from packages/data-pipeline/src
  </acceptance_criteria>
  <verify>
    <automated>cd packages/data-pipeline/src && python3 -c "
from synthetic_pop.dp_inject import prove_dp_guarantee, inject_dp_privacy, build_privacy_statement
from synthetic_pop.evaluate import evaluate_quality, evaluate_privacy, generate_quality_report
from synthetic_pop.export import export_synthetic_population, compute_sha256

# Verify no hand-rolled DP
import synthetic_pop.dp_inject as m
src = open(m.__file__).read()
assert 'import opendp' in src, 'Missing opendp import'
assert '.map(d_in=' in src or '.map(d_in' in src, 'Missing .map(d_in=) for formal proof'
assert 'numpy.random.laplace' not in src, 'Found hand-rolled DP!'

# Verify SHA-256 works
h = compute_sha256('pyproject.toml')
assert len(h) == 64, f'Bad hash length: {len(h)}'
print(f'OK: hash {h[:16]}..., no hand-rolled DP, .map() proof present')
"</automated>
  </verify>
  <done>dp_inject.py uses opendp with formal epsilon proof. evaluate.py runs SDMetrics. export.py writes versioned JSON with SHA-256.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real data file -> preprocess.py | CASD/INSEE microdata loaded from disk — gitignored, env-var path, no logging of records |
| CopulaGAN model -> synthetic output | Memorization risk — mitigated by OpenDP (epsilon <= 1.0) and SDMetrics DisclosureProtectionEstimate |
| PyPI -> pip install | SDV BUSL-1.1 license concern + supply chain — pinned versions in pyproject.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | preprocess.py — real data | mitigate | TOKENIZED_PATH env var + .gitignore. Logs only column names and row counts. |
| T-02-02 | Information Disclosure | train.py — memorization | mitigate | OpenDP Laplace (epsilon <= 1.0) + SDMetrics DPE score >= 0.7 threshold. |
| T-02-03 | Tampering | dp_inject.py — noise calib | mitigate | .map(d_in=1) is mechanical proof, not manual calculation. CI gate: actual_epsilon <= 1.0. |
| T-02-04 | Tampering | export.py — artifact integrity | mitigate | SHA-256 hash in meta.json. Plan 04 CI validates before Phase 2. |
| T-02-05 | Repudiation | dp_inject.py — privacy claims | mitigate | Every output includes actual_epsilon, mechanism name, and composition method in metadata. |
</threat_model>

<verification>
1. All 5 modules import without errors
2. No numpy.random.laplace in dp_inject.py (OpenDP formal proof only)
3. .map(d_in=) call present in dp_inject.py
4. SHA-256 compute works on a known file
5. QualityReport and DisclosureProtectionEstimate imports resolve
</verification>

<success_criteria>
- 5 pipeline modules import and function signatures verified
- OpenDP formal epsilon proof via .map() — NOT estimated or hand-rolled
- CopulaGAN configured with enforce_min_max_values, epochs=500
- SHA-256 integrity hash on export
- Real data path uses env var with .gitignore enforcement
</success_criteria>

<output>
After completion, create .planning/phases/01-data-foundation-rules-engine/01-02-SUMMARY.md
</output>
